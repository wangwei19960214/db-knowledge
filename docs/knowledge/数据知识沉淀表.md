# 数据知识沉淀表

本文件按“知识卡片”沉淀可复用取数经验。第一版只放已有项目文档和脚本中反复出现的高价值场景，不做全库学习。

## 知识卡片模板

| 字段 | 填写说明 |
| --- | --- |
| 卡片编号 | `K-业务域-序号` |
| 适用问题 | 用户会怎么问 |
| 业务目标 | 这次取数解决什么决策问题 |
| 已知数据源 | 表、视图、FineBI 数据集、脚本或报表 |
| 核心指标 | 指标名、公式、分子分母 |
| 常用维度 | 时间、平台、店铺、SKU、Model、国家、类目等 |
| 默认过滤 | 已验证的默认条件；未知写 `待核实` |
| 可复用 SQL/脚本 | SQL 模板、脚本路径或已有产物 |
| 校验方式 | 和哪个报表、FineBI、汇总数对账 |
| 坑点 | 重复、字段混用、粒度不一致、权限限制 |
| 状态 | `已验证` / `待核实` / `推断` / `权限不足` |

## K-WF-AD-001：Wayfair 广告组月度表现分析

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “看 Wayfair 广告组表现”“按 Campaign / SKU / 月份分析广告效果”“广告花费和利润是否合理” |
| 业务目标 | 对 Wayfair WSP 广告在月度、店铺、广告组、SKU 维度做投放效果、库存、销售和利润综合分析。 |
| 已知数据源 | `advertising_product_report_by_day`、FineBI `wf广告`、FineBI `WF店铺绩效2.0/广告`、`wf_class中文分类`、`zzdnb_wf上架sku每日销售额` / `wf上架sku每日销售额`、`detailed_listing_health`、`warehouse_stock_在售`、`wayfair上架sku销售映射`、`平台产品详情表`、`wf广告出价表`。 |
| 已有脚本 | `scripts/generate-wayfair-ad-group-analysis-data.js`、`scripts/build-wayfair-ad-group-analysis-workbook.py` |
| 已有产物 | `reports/固定流程取数和报表/REQ-003_Wayfair广告组库存综合分析/wayfair_ad_group_analysis_2026-01_2026-05/Wayfair_WSP广告组分析_2026-01_至_2026-05.xlsx` |
| 核心指标 | spend、WSC 广告销售额、订单数、销量、impressions、clicks、CTR、CVR、CPC、CPM、CPA、ROAS、ROI、总销售额、总利润、库存标签、有货率、评分、评论数。 |
| 关键口径 | WSC / wholesale cost 不是 retail sales；ROAS = WSC / spend；ROI = total profit / spend；比率指标汇总时必须按分子分母重算。 |
| 常用维度 | 月份、store、country、campaign_id、campaign_name、Wayfair SKU、采购 SKU、Model、class_name、中文分类、product_status、campaign_status、campaign_is_active、isB2b。 |
| 默认过滤 | WSP 广告、Wayfair 平台、指定月份范围；具体店铺范围按用户需求确认。默认不机械过滤 `isB2b` 或状态字段，除非用户明确要求或复刻具体 FineBI 组件。 |
| 常用关联 | Campaign 分析优先使用 `campaign_id`；`campaign_name` 只作展示或补充。SKU 粒度和销售、库存、评分联动时必须先确认上架 SKU 与采购 SKU 映射。 |
| SQL 模板 | `SQL模板库.md#sql-wf-ad-monthly-wayfair-广告组月度表现骨架` |
| 校验方式 | 与 FineBI 广告看板、脚本生成的 `intermediate/reconciliation.csv`、工作簿校验文件交叉检查。 |
| 2026-07-01 复核 | 广告事实 2026-01 至 2026-05 共 `135,395` 行，核心字段无空值；核心数值字段为字符串，汇总前需转数值。同月同 `store + campaign_id + sku` 多 `product_max_bid` 常见，`3,828` 组中 `718` 组多 bid。`wf_class中文分类` 69 行无重复，2026-01 至 2026-05 广告事实分类全命中。 |
| 2026-07-07 状态过滤和归因成熟度复核 | `isB2b=true` 和非 `ACTIVE/ACTIVE` 状态组合均有非零花费和 WSC；状态字段默认作为维度或异常说明，不作为广告表现查询的默认硬过滤。全表最新业务日期 `2026-07-06`，该日有 `1,095` 行和花费但 WSC / orders / units 为 `0`；`2026-07-05` 已有非零归因。近 14 天可有部分归因值，但 FineBI `是否满足14天` 口径下仍应标注 `归因未成熟`，正式 ROAS / 销售额结论默认排除或单独展示未成熟日期。 |
| 2026-07-08 新广告表规则 | `wf新广告表现` 相关物理表通常不带 FineBI `zzdnb_` 前缀；字段多为下划线名。WGS 查 `google_shopping` / `google_shopping_sku_level_attribution`，WSD 查 `sponsored_display_*`，WSS 查 `sponsored_shops_*`，WSV 查 `sponsored_video_*`。Sponsored 系列表默认先取最新 `create_time` 批次，`sponsored_display_sku_level_attribution` 即使最新批次也需按目标粒度聚合。 |
| BID 规则 | 历史广告效果分析使用 `advertising_product_report_by_day.product_max_bid`；当前 / 建议 bid 查 `wf广告出价表`。补当前 bid 时优先用 `campaignlayer_id = campaign_id`，`Campaign name` 只作展示或兜底。2026-01 至 2026-05 复核中 ID 匹配覆盖高于名称匹配，且 `WF-TD-CA`、`WF-TD-UK` 未见出价快照，不能默认用 `WF-TD` BID 回填。 |
| 利润规则 | `总成本/总利润` 不是广告日报原生字段；数据库字段搜索未发现当前 WSP 广告精确利润物理列。需要 FineBI 已计算样本或 DDP / 发货费 / 单买数量预聚合复算，不得从 `wf上架sku每日销售额` 或 `上架sku利润表` 直接推断。 |
| 坑点 | 广告明细和总销售额联表时容易把总销售额重复到多条广告记录；`product_max_bid` 是历史出价，不等同当前默认出价；按 `campaign_name` 关联 BID 会因名称变更丢匹配；默认过滤 B2B 或状态字段会低估历史表现；最新 / 近 14 天 Day_14 归因未成熟，不能直接当最终广告销售额 / ROAS；新广告表不能照抄 FineBI 展示名或全表直接汇总；利润字段需要公式链和样本 QA。 |
| 状态 | 广告事实、分类映射、BID 字段边界和状态过滤边界已复核；BID 覆盖率数据库侧已复核，CA / UK 出价兜底规则仍待核实；总成本、总利润 2026 样本数值 QA 仍待核实。 |

## K-WF-PRICE-001：Wayfair 当前价格优势 / 竞品价格查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “这个 SKU 有没有价格优势”“本品最低前台价是多少”“竞品最低价是多少”“广告报表补价格优势” |
| 业务目标 | 区分本品当前前台价、竞品最低价和历史价格优势，避免把当前快照或缺竞品价误写成历史广告月份结论。 |
| 已知数据源 | `产品近两天的最小前台价`、`link_sku_price_qtj_fabric`、`link_sku_table_2`、FineBI `本竞品价格对比`、FineBI `WF广告数据/广告`。 |
| 核心指标 | `本品最低前台价`、`竞品最低前台价`、`是否有价格优势`。FineBI 原式为竞品价低于本品价时“否”，否则“是”；严格业务口径为本品价低于竞品价才“是”。 |
| 常用维度 | store、platform、model、link_sku、产品类型、create_time。 |
| 默认过滤 | 当前价格查询默认用 `产品近两天的最小前台价` 最新快照；历史月份不得直接套当前快照。 |
| 可复用 SQL/脚本 | `SQL-WF-PRICE-ADVANTAGE-CURRENT`。 |
| 校验方式 | 先检查快照表是否同时存在 `产品类型='本品'` 和 `产品类型='竞品'`；再按 `本品 store + link_sku + model` 与竞品 `platform + model` 取最小价。 |
| 2026-07-02 补充校验 | 继续核查 `temp_link_sku_price_qtj_fabric`、`temp_latest_link_sku_price_scj`、`最新价格表`、`竞品对标表`、`本竞品失效sku`：前两张 temp 表当前回连后也只见本品；`最新价格表` 无竞品字段且价格日期偏旧；`竞品对标表` 只有关系无价格；`本竞品失效sku` 只有失效状态无价格。 |
| 2026-07-02 过程补充 | `link_sku_price_qtj_fabric` 过程定义仍有竞品分支；按 `link_sku_table_2.link_sku = product_fabric.sku` 的最新竞品映射样本，在 `product_fabric` 近 90 天仍能命中部分竞品价格，但这些竞品价没有落入 `link_sku_price_qtj_fabric` / `产品近两天的最小前台价` 当前结果。 |
| 2026-07-02 修复确认 | 用户确认本次竞品价未落入当前结果表的原因是相关函数/例程执行失败，现已修复；恢复后的竞品行数量、最新日期和 FineBI 对账样本待只读复核。 |
| 2026-07-02 修复后复核 | 下游快照 `产品近两天的最小前台价` 已恢复：`本品 10,485` 行、`竞品 4,649` 行，`create_time=2026-07-02 06:40:00`。目标 WF 店铺严格价格优势覆盖：`WF-FOP 244/194/52`、`WF-TD 727/707/255`、`WF-TD-CA 267/170/81`、`WF-TD-UK 378/75/17`，格式为本品 key / 命中竞品 key / 严格优势 yes。 |
| 2026-07-07 上游恢复复核 | 上游 `link_sku_price_qtj_fabric` 已恢复 `竞品` 行：`Wayfair 1,465,373`、`Wayfair-CA 73,323`、`Wayfair-UK 17,161`、`Overstock 688`，`max_create_time=2026-07-07 01:35:48`；下游快照 `产品近两天的最小前台价` 当前 `create_time=2026-07-06 06:40:00` 仍有竞品行。 |
| 2026-07-08 价格源表边界 | 当前快照 `产品近两天的最小前台价` 约 `14,800` 行，样本 `create_time=2026-07-07 14:40:00`，仍是默认价格优势入口；`link_sku_price_qtj_fabric`、`product_fabric`、`product_fabric_recovery`、`osprice`、`os_manage_cost` 均为大表或上游 / OS 候选，不作为普通智能问数默认入口。 |
| 2026-07-14 日期边界 | `link_sku_price_scj.日期` 直接继承 `wfprice.date`、`osprice.date` 或 Lowes 源创建日期，当前可见晚于系统日期的未来生效记录；不得用 `MAX(日期)` 判断刷新新鲜度。上传价链应联合检查 `CREATE_TIME`、事件最近执行时间，并按业务问题限制 `日期 <= 查询日`。当前 `产品近两天的最小前台价` 仍同时有本品 `10,488` 行和竞品 `4,631` 行。 |
| 坑点 | 当前结果表缺竞品价时不能返回“无价格优势”，只能返回“竞品价缺失 / 待核实”。函数修复后可以恢复当前快照口径价格优势计算，但仍不能用当前快照回填历史月份。`link_sku_price_qtj_fabric` 是大表且缺少 `产品类型` / `platform` / 单列日期索引，不作为默认高频智能取数入口；`wfprice` 是 Wayfair 成本 / 促销成本入口，不是竞品最低价入口。 |
| 状态 | 当前本品价入口、候选源边界、竞品未落表现象、函数失败原因、下游快照恢复、上游大表竞品恢复和大表选型边界已确认；FineBI 当前逐行样本一致性仍待复核。 |

## K-WF-INV-001：Wayfair 有货率 / 断货分析

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “Wayfair 哪些 SKU 断货”“上架 SKU 有货率”“链接 SKU 有货率”“哪些 SKU 有断货风险” |
| 业务目标 | 按上架 SKU、链接 SKU、店铺、国家和日期范围统计库存可用、断货、缺映射和异常情况。 |
| 已知数据源 | `warehouse_stock_在售`、`平台产品详情表`、`wayfair上架sku销售映射`、`仓库映射表`、`tb_kyxl_forecast_and_reality`、`考虑放弃sku清单`。 |
| 已有脚本 | `scripts/generate-wayfair-availability-data.js`、`scripts/build-wayfair-availability-workbook.py`、`scripts/build-wayfair-availability-workbook.mjs` |
| 已有产物 | `reports/固定流程取数和报表/REQ-004_Wayfair有货率报表/wayfair_availability_2026-06-15/`、`reports/固定流程取数和报表/REQ-004_Wayfair有货率报表/wayfair_availability_2026-01_to_2026-06-15/` |
| 核心指标 | 上架 SKU 有货率、链接 SKU 有货率、断货天数、可用库存、缺映射数量、异常清单。 |
| 关键口径 | 上架日计入、下架日不计入；采购 SKU 映射优先 `wayfair上架sku销售映射`，再用 `平台产品详情表.采购sku`；组合 SKU 任一组件库存小于等于 0 视为断货；店铺对应国家仓库；链接 SKU 聚合前过滤 `NULL`、空字符串和 `无`。 |
| 常用维度 | 日期、store、country、平台 SKU / 上架 SKU、链接 SKU、采购 SKU、仓库、断货原因。 |
| 默认过滤 | Wayfair 上架范围、指定日期范围；是否排除停用 / 放弃 SKU 需按需求确认。 |
| SQL 模板 | `SQL模板库.md#sql-wf-availability-wayfair-有货率骨架` |
| 校验方式 | 复用有货率工作簿中的汇总、明细、异常清单、校验文件。 |
| 坑点 | SKU 映射缺失、组合 SKU 拆解、国家仓库对应、无效链接 SKU、上架下架日期边界最容易造成口径差异；`仓库映射表.后台仓库名称='无'` 映多国家，不可作为有效国家仓。 |
| 本轮验证 | 2026-06-26 只读确认：Wayfair 上架行 `13,591`，其中 `10,058` 行命中专用映射，`1,669` 行可回退 `平台产品详情表.采购sku`，`1,864` 行无采购 SKU 可回退；专用映射采购 SKU 全部命中 `采购sku_model映射表`。 |
| 状态 | 有货率映射优先级、无效链接过滤、仓库映射去重规则已确认；`数量` 已确认是销售个数，预测断货来源刷新逻辑和取消数量多组件扣减 SQL 待核实。 |

## K-SKU-MAP-001：SKU / Model / 平台映射查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “这个 SKU 对应哪个 Model”“某 Model 在哪些平台/店铺上架”“Wayfair SKU 怎么找到采购 SKU” |
| 业务目标 | 建立平台 SKU、上架 SKU、采购 SKU、Model、店铺、国家之间的查询入口，支持销售、库存、广告和利润分析前置清洗。 |
| 已知数据源 | `平台产品详情表`、`wayfair上架sku销售映射`、`独立站上架产品管理`、`采购sku_model映射_2`、`warehouse_stock_在售`、`仓库映射表`。 |
| 核心字段 | 平台、店铺、国家、平台 SKU、上架 SKU、链接 SKU、采购 SKU、Model、仓库、状态字段。 |
| 关键口径 | 智能取数先识别输入对象类型；Model 命中时先走 `采购sku_model映射表.model -> 采购sku`。Wayfair 场景中，上架 SKU 到采购 SKU 组件优先使用 `wayfair上架sku销售映射`，其 `part_number` 对应 `平台产品详情表.上架sku`，不是 `平台sku` 或 `链接sku`；dreamsidea 独立站使用 `独立站上架产品管理`，按 `平台='Wordpress' + 店铺='dreamsidea' + 上架sku` 查 `采购sku/数量`；其他通用平台映射回到 `平台产品详情表`。 |
| 字段边界与刷新 | Wayfair：`链接sku=Display_SKUs`、`上架sku=Supplier_Part_Number`、`平台sku=SKU`；Overstock：`链接sku=sku`、`上架sku=supplier_sku`、`平台sku=full_sku`；Homedepot：`链接sku=Merchant_SKU/UPC_EAN`、`上架sku=Vendor_SKU`、`平台sku=Merchant_SKU`；Lowes：`链接sku=sku`、`上架sku=store_unique_identifier`。`平台产品详情表` 每日 09:50 更新，`采购sku_model映射表` 每小时 :10，`link_sku_table_2` 每小时 :40。`sku_model映射表` 未发现独立事件，不作为当前默认映射入口。 |
| 常用维度 | SKU、Model、平台、store、country、仓库、状态。 |
| SQL 模板 | `SQL模板库.md#sql-sku-model-platform-map-sku--model--平台映射骨架` |
| 校验方式 | 与具体业务场景的销量、库存、广告明细对账；检查 `store + 上架sku`、`store + part_number`、采购 SKU 和 Model 的一对多映射。 |
| 坑点 | 同一个 Model 可对应多个采购 SKU；同一采购 SKU 可被多个平台 SKU 引用；跨平台字段命名不完全一致。做 Model 库存查询时，不要直接查 `warehouse_stock_在售.sku = model`，应先映射到采购 SKU。`平台产品详情表.采购sku` 可能为空或逗号分隔多值；`采购sku_model映射表.model` 存在空值和疑似非标准值。 |
| ETL 坑点 | `平台产品详情表.采购sku_first` 来自未指定顺序的 `GROUP_CONCAT` 首项，多采购 SKU 产品不能把它当稳定业务主键；应拆分完整 `采购sku` 或使用平台专用销售映射。物理表 `采购sku_model映射表` 与 FineBI SQL 数据集 `采购sku_model映射_2` 不是同一对象。`采购sku_model映射表.model来源` 会在更新产品编码时被写成“工厂订货表产品编码”，不能单独作为 Model 来源审计字段。 |
| 2026-07-14 FineBI 映射边界 | 已按文档 SQL 在数据库侧只读复算 `采购sku_model映射_2`：结果 `8,117` 个采购 SKU，其中 `5,212` 个 Model 为空；与物理 `采购sku_model映射表` 比较有 `677` 个 Model 文本差异，其中 `621` 个仍包含物理 Model，真正不包含物理 Model 的冲突为 `56` 个。智能取数默认仍以物理表做采购 SKU -> Model 主入口；需要复刻 FineBI 库存看板时才使用 FineBI SQL 口径，并保留多 Model 字符串和空 Model。 |
| 本轮验证 | 2026-06-25 只读确认：`平台产品详情表` Wayfair `19,421` 行，`store + 上架sku` 未发现重复；`wayfair上架sku销售映射` `19,265` 行，主键为 `store + part_number + 采购sku`，存在 `3,736` 个多组件 `store + part_number`；`CH0016` 作为 Model 可映射到 7 个采购 SKU。2026-06-26 二次校验：Wayfair 产品详情增至 `19,705` 行，专用映射增至 `19,313` 行；专用映射的 `2,098` 个采购 SKU 全部命中 `采购sku_model映射表`。2026-07-07 只读确认：`独立站上架产品管理` 中 dreamsidea 为 `平台=Wordpress`、`店铺=dreamsidea`，按上架 SKU 可命中多采购 SKU 组合映射；样本 `OD0003-LIGHT CAMEL-2C+ST` 命中 2 个采购 SKU，`OD1001-BEIGE-3SW+OD1003-CT+LS` 命中 3 个采购 SKU。 |
| 状态 | Wayfair 库存 / 有货率映射链路已形成可复用选路规则；`数量` 已确认是销售个数；全平台通用映射规则、FineBI `采购sku_model映射_2` 与物理表差异、取消数量多组件扣减 SQL 仍待核实。 |

## K-DB-CATALOG-001：数据库对象地图只读元数据学习

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “先把数据库对象列出来”“哪些表最大”“有哪些视图/过程/触发器/事件”“数据库对象按业务怎么初步分类” |
| 业务目标 | 在不读取业务表数据、不深入学习全库逻辑的前提下，建立对象级地图，为后续按主题学习和智能取数选表提供入口。 |
| 已知数据源 | `information_schema.TABLES`、`information_schema.COLUMNS`、`information_schema.STATISTICS`、`information_schema.KEY_COLUMN_USAGE`、`information_schema.ROUTINES`、`information_schema.TRIGGERS`、`information_schema.EVENTS`。 |
| 已有产物 | `reports/固定流程取数和报表/SUPPORT_数据库对象地图/database_catalog_2026-06-25.xlsx` |
| 核心指标 | 对象数量、行数估算、表大小、字段数、主键情况、索引数量、是否有注释、最近更新时间、初步业务分类、疑似核心对象。 |
| 常用维度 | schema、对象类型、业务分类、是否疑似核心对象、状态。 |
| 默认过滤 | 仅当前业务 schema；只读 `information_schema`；不读取业务表数据；不读取过程/触发器/事件正文。 |
| 可复用 SQL/脚本 | 第一轮使用临时构建器生成 Excel；后续如重复执行，可固化为 `scripts/` 下只读元数据脚本。 |
| 校验方式 | 检查执行 SQL 家族仅为 `information_schema`；检查 Excel 页签完整；检查无连接参数；检查对象数量汇总。 |
| 坑点 | `TABLE_ROWS` 是估算值；视图行数通常不可用；对象名分类只能作为初判；过程、触发器、事件不读正文时不能确认血缘。 |
| 状态 | 可复用方法已验证；对象业务归属和血缘仍需按主题待核实。 |

## K-DB-CATALOG-002：数据库对象清单与旧文档知识对齐

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “已有数据库对象清单，怎么结合项目文档判断哪些表值得先学”“哪些对象已经被旧需求用过”“对象清单如何服务智能取数选表” |
| 业务目标 | 在不重新查库的前提下，把旧文档里的数据库、FineBI、报表和知识卡片经验回填到对象清单，形成更接近业务语境的精学优先级。 |
| 已知数据源 | `reports/固定流程取数和报表/SUPPORT_数据库对象地图/database_catalog_2026-06-25.xlsx`、`docs/summaries/business-summary.md`、`docs/business/数据表目录.md`、`docs/business/指标口径字典.md`、`docs/business/业务流程需求沉淀.md`、`docs/business/已知坑点.md`、`docs/finebi/`、`docs/knowledge/`、`docs/playbooks/`。 |
| 已有产物 | `reports/固定流程取数和报表/SUPPORT_数据库对象地图/database_catalog_enriched_2026-06-25.xlsx` |
| 核心指标 | 旧文档命中对象数、已复用对象数、文档命中但待核实对象数、旧文档未覆盖但疑似核心对象数、FineBI 相关对象数、知识卡片关联对象数、高优先级精学对象数。 |
| 常用维度 | 对象名称、对象类型、初步业务分类、命中文档、命中标题或片段、已有业务主题、复用状态、下一步学习优先级。 |
| 默认过滤 | 只使用既有 Excel 和 Markdown 文档；不重新查库；不读取业务数据；不确认过程/触发器/事件正文。 |
| 可复用方法 | 先用对象名与旧文档做保守命中，再结合命中文档类型、标题和旧分类判断 FineBI / 脚本 / 知识卡片相关性；不确定内容统一标 `待核实`。 |
| 校验方式 | 检查增强版 Excel 页签；检查原始 Excel 未删除；检查是否未连接数据库；检查产物和文档不包含真实连接参数。 |
| 坑点 | 汇总段落可能让对象误命中过多业务主题，因此主题判断应优先采用原始分类、命中文档类型和命中标题，不把整篇文档主题直接套到对象上。 |
| 状态 | 可复用方法已建立；具体对象字段、刷新逻辑和血缘仍需按业务主题继续学习。 |

## K-WF-INV-002：库存 / 有货率 / 断货主题对象地图

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “库存主题应该先学哪些表”“有货率、断货、CG 仓库存分别用哪些对象”“做库存取数前要确认什么” |
| 业务目标 | 把库存 / 有货率 / 断货主题拆成主事实、映射、结果表、FineBI 对账和待核实对象，作为后续真实取数的选表入口。 |
| 已知数据源 | `warehouse_stock_在售`、`warehouse_stock_最新`、`平台产品详情表`、`wayfair上架sku销售映射`、`仓库映射表`、`tb_kyxl_forecast_and_reality`、`各sku每月断货天数`、`zzdnb_断货sku数量`、`wayfair后台cg可用`、`cg_part_sku映射`、`考虑放弃sku清单`。 |
| 核心指标 | 可用库存、上架 SKU 有货率、链接 SKU 有货率、断货天数、断货开始/结束日期、断货数量、断货 SKU 数量、CG 仓可用、在途。 |
| 常用维度 | 日期、年月、store、country、仓库、上架 SKU、链接 SKU、采购 SKU、Model、是否合并 SKU。 |
| 默认过滤 | Wayfair 有货率默认按在架商品、店铺对应国家仓、指定日期范围；断货预测默认排除 `考虑放弃sku清单`。 |
| 可复用 SQL/脚本 | `docs/knowledge/SQL模板库.md#sql-wf-inventory-theme-availability-first-pass库存主题--wayfair-有货率第一轮骨架`、`docs/knowledge/SQL模板库.md#sql-wf-cg-inventory-first-passcg-仓库存骨架`、`docs/knowledge/SQL模板库.md#sql-stockout-monthly-finebi-first-pass月度断货明细骨架`。 |
| 校验方式 | 与 FineBI `仓库库存可用预估`、`cg仓库存`、Wayfair 有货率报表汇总/异常清单对账；检查库存、销量、广告数据截止日期是否一致。 |
| 坑点 | 库存主表有历史、实时、拆分多套；组合 SKU 必须看所有组件；月度 KPI 表不能替代分日事实；字段名和生成过程未确认前不得写成最终 SQL。 |
| 状态 | 第一轮主题地图已建立；`warehouse_stock_在售` 字段、粒度、唯一键和可用口径已完成单表确认，刷新逻辑、实时表边界和映射规则仍需继续核实。 |

## K-WF-INV-003：`warehouse_stock_在售` 库存历史事实表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某个采购 SKU 某天各仓可用库存是多少”“Wayfair 有货率的库存事实表怎么取”“库存历史表一行代表什么” |
| 业务目标 | 固定 `warehouse_stock_在售` 的字段边界、粒度、唯一键和可用库存判断，为 Wayfair 有货率、库存查询和断货排查提供库存事实入口。 |
| 已知数据源 | `warehouse_stock_在售`、`仓库映射表`；有货率场景还需 `平台产品详情表`、`wayfair上架sku销售映射`。 |
| 核心指标 | `SUM(可用)`、`SUM(待出库)`、`SUM(实际标品库存)`、`SUM(在途)`、有货标记 `SUM(可用) > 0`。 |
| 常用维度 | `date`、`sku`、`仓库`、国家、SKU 状态、category。 |
| 默认过滤 | 指定库存日期；是否排除 `sku状态` 为停用或空值需按报表确认，当前标 `待核实`。 |
| 可复用 SQL/脚本 | `docs/knowledge/SQL模板库.md#sql-sku-inventory-basicsku-库存查询骨架`、`docs/knowledge/SQL模板库.md#sql-wf-availabilitywayfair-有货率骨架`。 |
| 校验方式 | 字段目录、物理主键、最新日期轻量聚合、`可用` 空值/负数检查、仓库国家映射去重检查。 |
| 坑点 | 粒度是 `date + sku + 仓库`，不是 Model、平台 SKU 或链接 SKU；Model 库存查询需先映射到采购 SKU；`仓库映射表` 直接 join 会因多行映射放大库存；`part` 部分为空，`s-sku` 最新日期全空；月度断货 KPI 不能直接用本表替代。 |
| 状态 | 核心字段、物理粒度、候选唯一键、可用库存口径已验证；与实时表同步、停用 SKU 过滤、断货预测结果表关系仍待核实。 |

## K-WF-INV-004：指定 Model / 链接 SKU 历史与后续预计有货率

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某个 model 所有组合链接 1-5 月有货率怎么样”“某批链接后续预计有货率如何”“历史链接有货率和预测可用能否合并出表” |
| 业务目标 | 以指定 model / SKU 关键词为范围，输出 `店铺 + 链接SKU` 粒度的历史月度有货率、期间加权有货率和未来预测有货率。 |
| 已知数据源 | 历史优先复用 `reports/固定流程取数和报表/REQ-004_Wayfair有货率报表/wayfair_availability_2026-01_to_2026-06-15/wayfair_availability_data.json`；预测使用 `平台产品详情表`、`wayfair上架sku销售映射`、`tb_kyxl_forecast_and_reality`、`考虑放弃sku清单`。 |
| 核心指标 | 月度链接 SKU 有货率、1-5 月加权有货率、后续预计有货率、缺失权重占比、预测在架天数、预测最大在架 SKU 数。 |
| 关键口径 | 历史链接 SKU 有货率沿用有货率报表口径；预测按采购 SKU 组件判断，任一组件 `可用<=0` 或 `预估可用<=0` 视为断货；链接层使用最近历史销量权重，权重为 0 时等权回退。 |
| 默认过滤 | 指定关键词，例如 `CS0002`；历史默认取 2026 年 1-5 月；后续预测若用户未指定，默认未来 3 个月并标注默认假设。 |
| 可复用脚本 / 产物 | CS0002 专项产物为历史记录，当前磁盘未找到；如需恢复，先按 `reports/INDEX.md` 的 REQ-009 预期归类路径确认脚本和产物状态。 |
| 校验方式 | 检查 `店铺 + 链接SKU` 唯一；检查每个链接预测分段完整；检查历史月份覆盖；检查预测缺失权重占比；检查工作簿公式错误和预览。 |
| 坑点 | `tb_kyxl_forecast_and_reality` 是采购 SKU 级预测，本轮未确认国家仓 / 店铺仓拆分；存在 `考虑放弃sku清单` 命中时，不应静默排除或静默计入，需在备注中标出并待业务确认。 |
| 状态 | CS0002 场景有历史产物线索，当前磁盘未找到；预测国家仓口径和放弃 SKU 处理仍待核实。 |

## K-WF-INV-005：库存主表边界与智能取数选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU 5 月每天库存是多少”“某 SKU 现在还有多少库存”“CG 仓 / AMZ / 垂直库存怎么查”“为什么当前库存和历史库存数字不一样” |
| 业务目标 | 固定 `warehouse_stock_在售`、`warehouse_stock_最新`、`库存拆分记录` 的使用边界，减少智能取数误选表。 |
| 已知数据源 | `warehouse_stock_在售`、`warehouse_stock_最新`、`库存拆分记录`；Wayfair 有货率另需 `平台产品详情表`、`wayfair上架sku销售映射`、`仓库映射表`。 |
| 核心指标 | `可用`、`待出库`、`实际标品库存`、`在途`、`总可用库存`、`cg仓可用库存`、`其他仓库可用库存`、`amz可用库存`、`垂直可用库存`。 |
| 选表规则 | 历史库存 / 日期范围默认 `warehouse_stock_在售`；当前库存默认每小时 `:05` 重建的 `warehouse_stock_最新`；`warehouse_stock_最新_temp` 是 12 小时多批次缓冲，不是当前主表。拆分库存原默认 `库存拆分记录`，但该表当前停在 2026-07-08，恢复前不得用它回答当前拆分库存；Wayfair 有货率仍用历史表加映射链路。 |
| 过滤与刷新边界 | `warehouse_stock_在售` 只硬过滤零件 / 分销 / 不统计 / 面料一级分类，不自动删除停用 SKU；每日 04:05 后进入源表的当日记录不会实时补入。需要“当前库存”时查每小时快照，需要“在售 SKU 库存”时还要按业务确认是否显式过滤 `sku状态`。 |
| 可复用 SQL/脚本 | `docs/knowledge/SQL模板库.md` 中的 `SQL-SKU-INVENTORY-BASIC`、`SQL-SKU-INVENTORY-CURRENT`、`SQL-SKU-INVENTORY-HISTORICAL-DAILY`、`SQL-SKU-INVENTORY-SPLIT-DAILY`。 |
| 校验方式 | 先识别输入对象是否为采购 SKU / Model / 上架 SKU / 链接 SKU；再按选表规则取数；输出来源表、日期字段、最新日期和待核实项。 |
| 坑点 | `warehouse_stock_最新` 和 `warehouse_stock_在售` 都可能出现当天日期，但同步时点和仓库覆盖不同；`库存拆分记录` 是 `sku + date` 粒度，没有原始 `仓库` 字段；拆分表存在负库存需标异常；Model 不可直接查库存表 `sku`。 |
| 状态 | 2026-07-13 已确认当前库存每小时生成链、每仓最新日期规则和缓冲表边界；拆分快照的停更根因已确认但修复 / 补数待执行。 |

## K-WF-INV-006：Wayfair CG 仓库存查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某个 Wayfair part / 上架 SKU 在 CG 仓还有多少”“CG 仓可用和在途是多少”“后台 CG 可用和库存快照为什么不一致” |
| 业务目标 | 固定 CG 仓当前库存的取数入口、粒度、映射方式和异常处理，支持智能取数回答 CG 仓库存问题。 |
| 已知数据源 | `wayfair后台cg可用`、`cg_part_sku映射`；对比时可参考 `warehouse_stock_最新` 的 CG 仓字段，但不能直接替代。 |
| 核心指标 | `available` / 可用、`in_stock` / 实际标品库存、`on_order` / 在途、未映射 part 数、未映射可用库存。 |
| 常用维度 | `create_time` 日期、store、part_number / 上架 SKU、采购 SKU、source warehouse、status。 |
| 关键口径 | 默认取 `wayfair后台cg可用` 最新 `create_time` 日期；源表粒度接近 `store + part_number + warehouse + 日期`；采购 SKU 通过 `store + part_number = store + 上架sku` 关联 `cg_part_sku映射`。 |
| 可复用 SQL/脚本 | `docs/knowledge/SQL模板库.md#sql-wf-cg-inventory-first-passcg-仓库存骨架`。 |
| 校验方式 | 检查最新日期、`ACTIVE / INACTIVE` 分布、未映射 part、映射后行数是否扩展、按 store 汇总与 FineBI / `warehouse_stock_最新` 的差异。 |
| 坑点 | `cg_part_sku映射` 是多组件视图，左连会扩展行数；如果按采购 SKU 展开后直接汇总库存，可能重复计算。`status` 是否过滤待核实；`wayfair后台cg可用` 与 `warehouse_stock_最新` CG 仓口径不完全一致。 |
| 2026-06-26 对账补充 | `warehouse_stock_最新` 的 CG 仓 `在途` 当前基本等于 `ACTIVE + 已映射 on_order`；CG 仓 `可用` 当前基本等于 `ACTIVE + 已映射 + 排除 90670 源仓 available`。该关系为样本验证 / 生成线索，不等于已确认血缘。 |
| 状态 | 2026-06-26 已确认字段、最新日期、粒度、覆盖率、join 放大风险和 CG 快照差异主因线索；FineBI 组件层是否额外过滤、90670 业务归类、`库存拆分记录` 差异和生成过程仍待核实。 |

## K-WF-INV-007：库存预测 / 断货 / 拆分链路选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU 未来会不会断货”“某月断货 SKU 数是多少”“每天 CG / AMZ / 垂直库存怎么解释”“售罄预估字段从哪来” |
| 业务目标 | 区分预测事实、月度 KPI、库存拆分、CG 每日库存和售罄预估结果表，避免智能取数误用重视图或 KPI 表反推明细。 |
| 已知数据源 | `tb_kyxl_forecast_and_reality`、`tb_kyxl_forecast_and_reality_合并sku`、`tb_kyxl_forecast_and_reality_amz`、`tb_kyxl_forecast_and_reality_垂直`、`断货sku数量`、`断货sku数量_合并`、`考虑放弃sku清单`、`库存拆分记录`、`cg仓每天库存`、`cg仓库库存借调`、`仓库每日销量断货情况_表`、`每月销量售罄预估表`。 |
| 选表规则 | 未来断货分日查 `tb_kyxl_forecast_and_reality`；月度整体 KPI 查物理表 `断货sku数量`，FineBI 显示名为 `zzdnb_断货sku数量`；每天拆分库存通常查 `库存拆分记录`，但必须先校验最大日期，当前停在 2026-07-08，不能回答实时问题；CG 店铺 / 仓库 / 上架 SKU 历史查 `cg仓每天库存`；当前 Shipout 查 `_2` 系列；售罄预估先查基础结果表。 |
| 关键口径 | `tb_kyxl_forecast_and_reality` 是 `date + sku` 粒度，当前未见 store / 国家 / 仓库字段；`库存拆分记录` 是 `sku + date` 粒度，仓库轴和渠道轴字段不能全部相加；`仓库每日销量断货情况_表.是否断货` 存值为 `正常` / `断货` / `NULL`。 |
| 可复用 SQL/脚本 | `docs/knowledge/SQL模板库.md` 中的 `SQL-STOCKOUT-RISK-BASIC`、`SQL-STOCKOUT-MONTHLY-FINEBI-FIRST-PASS`、`SQL-SKU-INVENTORY-SPLIT-DAILY`。 |
| 校验方式 | 输出前说明表粒度、时间字段、是否为物理表还是 FineBI 显示名；预测类查询检查 `可用` / `预估可用` NULL 含义；拆分类查询检查负库存和拆分字段是否误加总。 |
| 坑点 | `zzdnb_断货sku数量` 是 FineBI 名称，不一定是数据库物理表；事件最近执行不等于目标表刷新成功；`产品库存拆分` 当前因简码一对多产生同 SKU 重复，`库存拆分记录保存` 会触发主键冲突；过程 `shipout每天库存` 实际写 `_2` 系列，不能按过程名猜目标表。 |
| 状态 | 2026-07-13 已确认预测、月度 KPI 和拆分主链路及关键公式；`库存拆分记录` 当前停更根因已确认。修复补数、小时批次时序、国家 / 店铺拆仓和 FineBI 组件口径仍待核实。 |

## K-SALES-ORDER-001：通用订单销售明细 / `order_details_tb`

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU 销量 / 销售额是多少”“按平台 / 店铺 / 月看销售”“Model 销量怎么取”“订单利润查询的销售底表是什么” |
| 业务目标 | 固定通用订单销售事实入口，区分销售额、销量、日期回退和利润候选视图边界。 |
| 已知数据源 | `order_details_tb`、`order_details_tb_profit`、`order_details_tb_tj`、`order_details_tb抽取记录`。 |
| 核心字段 | `订单日期`、`做单日2`、`出单平台`、`出单店铺修`、`PO新修`、`POstg`、`sku修`、`数量`、`售价`、`总销售额`、`category`、`发货仓库`。 |
| 关键口径 | SKU 销量 / 销售额默认查 `order_details_tb`；销量用 `数量`，销售额用 `总销售额`，日期优先用 `订单日期`。`order_details_tb_tj` 的日历补齐 / 历史回退规则为：`订单日期 IS NULL OR 订单日期 < '2023-12-01'` 时使用 `做单日2`。 |
| 利润边界 | `order_details_tb_profit` 是基于 `order_details_tb` 的视图，只补 `平台折后销售额` 和 `国家`；不是最终利润表。最终 `利润 / 总利润` 仍需 DDP / 佣金 / 出货费下游公式。 |
| 可复用 SQL/脚本 | `SQL-SKU-SALES-BASIC`、`SQL-MODEL-SALES-BASIC`。 |
| 校验方式 | 检查日期范围、`订单日期` 空值、`总销售额` 与 `数量 * 售价` 差异、零销售额、平台 / 店铺分布、是否需要排除分销。 |
| 坑点 | `总销售额` 不能默认重算为 `数量 * 售价`；本表未见取消 / 退款字段；`order_details_tb_tj` 较重，不作为默认高频入口，除非需要 SKU 日历补齐或历史日期回退。 |
| 状态 | 主表字段、主键、日期、销售额和利润候选视图边界已确认；退款 / 取消扣减和最终 DDP 利润公式待核实。 |

## K-SALES-DDP-001：DDP 利润 / 退款 / 补件候选链路

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU / Model 利润是多少”“利润为什么和销售额对不上”“退款 / 退货 / 补件是否扣了”“净销量怎么算” |
| 业务目标 | 固定 DDP 利润智能取数的候选数据源和边界，避免把 `order_details_tb_profit` 或 `上架sku利润表` 误当最终利润表。 |
| 已知数据源 | 销售候选 `order_details_tb_profit`；月度 DDP 成本 `ddp汇总表_美国/加拿大/英国`；每日上架成本快照 `上架sku利润表`；发货费候选 `产品发货费`、CG / 魔方仓发货费；亏损告警 `钉钉利润提醒表`；退款 / 退货候选 `*_return_sal`、`wf退款明细`；月度扣减候选 `退货补件费用`。`店铺月利润` 当前失效，只保留历史线索。 |
| 核心字段 | `order_details_tb_profit.总销售额 / 平台折后销售额 / 国家`；`上架sku利润表.出单价 / 平台佣金 / 发货费 / ddp / 成本 / 利润率上限`；`homedepot上架sku销售映射.ddp价格 / base_cost`；`产品发货费.出货费用`；`店铺月利润.ddp / 美西出货费用 / 美东出货费用 / 净利润`；`退货补件费用.退货金额 / 补件费用`。 |
| 关键口径 | `order_details_tb_profit` 不是最终利润表；三国 DDP 采用实际优先、预计其次、价格记录兜底并沿用最近有效值；`上架sku利润表.成本=发货费+ddp`，不含平台佣金。`钉钉利润提醒表.总利润=平台折后销售额-发货费-DDP`，但仅保留亏损 PO。以上对象粒度不同，不能直接互换。 |
| 价格与佣金边界 | `link_sku_price_scj` 每日 09:30 / 14:30 重建，`上架sku利润表` 12:42 使用 09:30 批次。HD 当前过程写空价格；历史上传价最晚停在 2024-05-30，当前可用的是单独的本品前台价链，不能互相替代。`order_details_tb_profit` 对 WF-TD-UK 明确按 6% 扣减，但 `上架sku利润表` 的佣金 CASE 漏掉 UK，且当前 `wfprice -> link_sku_price_scj` UK 价格全部为 0；只补 CASE 仍不能得到有效佣金。Lowes 依赖 `unit_prices`。价格有效性必须同时检查 NULL 和 0。当前缺 DDP 行在当月精确 SKU 和去 `-E1` 回退均未命中。 |
| WFCG 发货费边界 | `wfcg_calculate_all` 每日 10:20 从 Wayfair 上传尺寸、产品分类、平台采购 SKU 和重量生成 `wfcg_results`。`上架sku利润表` 将其中 `Pick_Only_Fee` 用作 Homedepot / Overstock 的 CG 多渠道候选，将 `Pick_Ship_Fee` 用作 Lowes 候选，并与 `产品发货费` 取较大值；Wayfair 本身仍走后台 / `cg仓发货费` 候选。WFCG 只影响发货费，DDP 仍来自三国月度 DDP 表。 |
| 组合 SKU 成本边界 | 组合 SKU 会拆成组件后分别累计发货费、DDP 和成本。若某个组件缺 DDP，`发货费` 仍会累计该组件，但 `(发货费 + DDP)` 因 DDP 为 NULL 不进入 `成本` 的 `SUM`，会形成部分成本。2026-07-13 加拿大快照有 `3` 行此类记录，均已被 `无ddp采购sku` 标记；不得把非空 `成本` 视为组合组件全部覆盖。 |
| Lowes 价格 / 佣金确认 | 当前上传价优先查 `link_sku_price_scj` 最新业务日并保留 NULL；`平台产品详情表.上传价` 可能保留旧价。订单佣金查 `lowes_orders`，按 `order_number` 汇总；全部订单行满足 `commission_excluding_taxes = amount * 15%`。对账使用 `order_number = order_details_tb.POstg` 并现场重算差异，不固化“87 单”：2026-07-14 当前为 92 单，其中 16 单行数 / 数量放大、76 单形状相同但售价不同。库内物理装载只确认 `总账单详情_details_stg -> zzdxq_details_stg_ddrq -> details -> order_details_tb`；历史和 7 月 14 日双批次确认前置按月份批量装载，但实现 / 调度仍待核实。 |
| 取消 / 退款 | 取消数量优先查 `总账单详情_extract.订单状态 / 取消数量`；退款 / 退货金额按平台查 `*_return_sal` 或 `wf退款明细`，月度汇总可看 `退货补件费用`。 |
| 可复用 SQL/脚本 | `SQL-SALES-DDP-PROFIT-CANDIDATE`。 |
| HD DDP 候选规则 | 2026-07-06 样本验证：2026-06 HD 订单拆分事实按 `出单店铺 + 上架sku + SKU` 可 100% 回连 `homedepot上架sku销售映射.store + part_number + 采购sku`；`ddp价格` 与 `产品最新ddp值.最新ddp` 高度一致，可作为 HD DDP 候选成本优先入口。 |
| HD 佣金 / 发货费候选规则 | 2026-07-06 样本验证：HD 2026-06 可用 `总销售额 - 平台折后销售额` 作为 3% 佣金候选。2026-07-13 又确认 `产品发货费` 由 `ship_out产品管理 + 出仓费` 计算，当前 `7,260` SKU 费用无空且同 SKU 多仓源行的计算费用无冲突，可作出货费用候选入口；仓库 / 重量展示列不能视为唯一源行。 |
| 退款 / 补件候选规则 | 2026-07-07 样本验证：2026-06 `退货补件费用.平台=垂直 / 国家=美国` 的两个非零退货 SKU 与 `hd_return_sal` 两条记录金额一致、符号相反；其他垂直平台退款表当月聚合为空。该月度表没有店铺 / 订单 / PO 字段，不能直接拆 HD 店铺补件费用。 |
| 坑点 | `上架sku利润表` 是成本快照，不是订单利润；当前 HD 全部缺价格 / 佣金、Lowes 少量价格 / 佣金为空、英国价格为 0 且佣金全空，部分 SKU 缺 DDP。HD 的历史非空上传价不会被当前空价清除，按非空判断会误当成新价格；UK 也不能用非 NULL 判断价格完整。UK 订单视图和上架快照的佣金规则不一致，不能跨表混算。`钉钉利润提醒表` 只增量保存亏损 PO、无订单日期和数据库内调度，不能汇总公司总利润。`店铺月利润` 回款源过期、扣款金额全空且使用旧固定补件费，当前为失效入口。退货金额符号仍需按平台确认。 |
| 状态 | 已确认 DDP、每日上架成本、亏损提醒、HD 库内不存在持续有效上传价源、UK 订单层 6% 候选规则、Lowes 原始 15% 佣金及库内 staging 后半链。最终 `ddp_利润查询` 字段级选源、HD 库外正式价源、UK 佣金业务确认、Lowes 库外装载器 / 历史错价输入、亏损提醒外部调度和退款补件并入口径仍待核实。 |

## K-SALES-001：组合产品销量权重 / `总账单详情_extract`

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “组合产品销量有货率怎么取销量权重”“某上架 SKU 拆到哪些采购 SKU 出货”“总账单详情抽取表能不能用于组件销量” |
| 业务目标 | 使用平台订单抽取后的采购 SKU / 组件明细，为组合产品有货率、销量权重、组件库存消耗和缺映射排查提供销售入口。 |
| 已知数据源 | `总账单详情_extract`、`wayfair_orders_allinfo`、`wayfair_orders_allinfo_api`、`鲸汇wayfair订单管理`、`overstock_orders_allinfo`、`homedepot_orders_allinfo`、`wayfair上架sku销售映射`、`overstock上架sku销售映射`、`homedepot上架sku销售映射`、`wfproduct_management_latest`、`总账单发货仓库映射`、`上架sku销售映射缺失记录`。用户口头称“销量映射表”的三张对象在数据库中真实表名为“销售映射”；业务确认 OS / HD 为组合件对应采购 SKU。WF 映射表的 `part_number` 是店铺 SKU / Supplier Part Number，可能是 `Kit`、`Standard` 或 Component，并映射到采购 SKU。 |
| 核心字段 | `出单平台`、`出单店铺`、`PO`、`订单日期`、`上架sku`、`SKU`、`数量`、`售价`、`sku总价`、`订单总价`、`订单状态`、`取消数量`、`订单来源`、`上架包含采购种类`。 |
| 关键口径 | 表粒度为 `出单店铺 + PO + 上架sku + SKU`；`SKU` 是采购 SKU / 组件；2026-06-29 用户业务确认 `数量` 是销售个数，`取消数量` 必须扣减；组件金额优先用 `售价 * 数量`。Wayfair 场景下 `上架sku` 是订单拆分后的子件 part number，不是 `wf上架sku每日销售额.supplier_part` 的销售 SKU 层。2026-06-30 CS0007 报表样本中，Overstock / Homedepot 的近 90 天和月销量以本表 `订单日期` 为销售事实，再用各自上架 SKU 销售映射表归因到采购 SKU。 |
| 常用维度 | 平台、店铺、订单日期、PO、上架 SKU、采购 SKU、订单来源、订单状态。 |
| 默认过滤 | 按业务指定日期范围；做有效销量时必须扣减 `取消数量`，并按业务要求排除或标注 `Cancelled/CANCELLED`、`Rejected`、`Returned` 等状态。 |
| SQL 模板 | `SQL-SALES-COMBO-EXTRACT-BASIC` |
| 校验方式 | 检查日期范围、主键粒度、`COUNT(DISTINCT SKU)` 组件数、`SUM(售价*数量)` 与去重后上架 SKU 金额差异、取消数量分布和缺映射记录。 |
| 2026-07-01 复核 | `总账单详情_extract` 当前 `57,889` 行，覆盖 `2026-04-01` 至 `2026-07-01`，主键重复数 `0`。取消记录中存在多组件订单上架 SKU 的 `取消数量` 在组件行重复，不能直接逐行 `数量 - 取消数量`。 |
| 2026-07-01 SQL 模板 | 新增 `SQL-SALES-EXTRACT-EFFECTIVE-QTY`。OS / HD 有效销量样本验证公式：先用映射数量还原订单上架 SKU 件数，再扣 `取消数量`，最后乘回组件 `map_qty`。2026-06 Overstock 直接逐行扣减会多算 `20` 件，其中 `7` 件来自 `map_qty > 1` 的取消扣减错误，`13` 行来自缺映射 / 映射异常。 |
| 坑点 | 多组件行会重复 `sku总价` 和 `订单总价`；不能直接按行求和。`上架包含采购种类` 有大量空值，识别组合应优先按实际 `COUNT(DISTINCT SKU)`。`wf上架sku每日销售额.supplier_part` 是销售源 Supplier Part Number / 销售 SKU 层，`总账单详情_extract.上架sku` 是订单子件层，不能直接等值 join 当同粒度。OS / HD 抽取中 `数量` 已经是订单数量乘映射数量后的组件销量，`取消数量` 是上架 SKU 层取消件数；多组件扣减时应先还原上架 SKU 件数再扣取消，再乘回组件数量。 |
| 状态 | 2026-06-26 表结构、触发器、过程、事件和轻量聚合已确认；2026-06-29 业务确认 `数量` 是销售个数、`取消数量` 必须扣减；2026-07-01 已复核当前覆盖、主键和取消数量重复风险；OS / HD 有效销量 SQL 已样本验证，Wayfair 正式分摊 SQL 和 FineBI 对应关系仍待核实。 |

## K-SALES-MAP-001：三平台上架 SKU 销售映射 / part number 到采购 SKU

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某平台 part number 对应哪些采购 SKU”“组合产品销量有货率该用哪张映射表”“订单拆分为什么某些 SKU 没映射”“WF 组合件销售额怎么继续分摊” |
| 业务目标 | 固定三平台 part number -> 采购 SKU 的映射入口、平台层级差异和刷新链路，避免把组合件、子件、采购 SKU 混到同一粒度。 |
| 已知数据源 | `wayfair上架sku销售映射`、`overstock上架sku销售映射`、`homedepot上架sku销售映射`；上游维护源为 `鲸汇店铺销售产品最新`，补充来源包括 `wfproduct_management_latest`、`产品最新ddp值`。`wfproduct_management_latest.Related_Kit_Part` 已由业务确认为 Wayfair 组合名字 / 组合件 part number，Component 行的 `Supplier_Part_Number` 为子件名称。 |
| 核心字段 | `store`、`part_number`、`采购sku`、`数量`、`product_sku`、`product_class`、`product_type`、`product_status`、`base_cost`、`ddp价格`、`来源`、`create_time`、`update_time`。 |
| 关键口径 | 三张表主键均为 `store + part_number + 采购sku`。Wayfair 映射表的 `part_number` 是店铺 SKU / Supplier Part Number，可对应 `Kit`、`Standard`、`Sellable Component` 或 `Nonsellable Component` 并映射到采购 SKU；父子层级要结合 `wfproduct_management_latest.Product_Type` 和 `Related_Kit_Part` 判断。Overstock / Homedepot 映射为组合件 part number -> 采购 SKU。OS / HD 两张映射表不含订单日期 / 月份，不能单独统计近 90 天或月销量；需要以 `总账单详情_extract` 做销售事实再用映射表归因采购 SKU。`wf上架sku每日销售额.supplier_part` 同样是销售源 Supplier Part Number / 销售 SKU 层，但并不保证与映射表同店铺、同状态、同 Product_Type 完整一致，正式 join 需检查一对多和未匹配。2026-06-29 用户业务确认映射表 `数量` 是销售个数。 |
| 常用维度 | 平台、store、part_number、采购 SKU、产品分类、产品状态、来源。 |
| 默认过滤 | 一般使用 `来源='鲸汇店铺销售产品最新'` 的最新刷新结果；如做订单覆盖率，按平台分别与 `总账单详情_extract.出单店铺 + 上架sku + SKU` 校验。 |
| SQL 模板 | `SQL-PLATFORM-LISTING-PURCHASE-MAP-PRECHECK` |
| 校验方式 | 查表行数、`store + part_number + 采购sku` 主键重复、`part_number` / `采购sku` / `数量` 空值或非数字、`store + part_number` 组件数分布、与 `总账单详情_extract` 的未命中 key。 |
| 2026-07-01 复核 | 当前画像：Wayfair `19,346` 行 / 多组件 `3,736`；Overstock `24,728` 行 / 多组件 `4,674`；Homedepot `5,511` 行 / 多组件 `964`。2026-06 抽取 key 对比映射，Overstock 缺 `5` 个 key，Wayfair / Homedepot 未见缺 key。 |
| 坑点 | Wayfair 过程当前把 `WF-TD` 映射复制派生到 `WF-TD-CA`、`WF-TD-UK`；需要业务确认。OS / HD 映射表没有日期字段，直接拿它们做月销量会把映射数量误当销量。直接用 `wf上架sku每日销售额.supplier_part = 总账单详情_extract.上架sku` 虽然会命中大量重名 key，但层级错误。`wayfair上架sku销售映射` 可包含 `Kit` 行和 Component 行，不能仅凭映射表一行判断父子关系；应先用 `wfproduct_management_latest` 的 Component 行把组合件 `Related_Kit_Part` 展开到子件 `Supplier_Part_Number`，再接采购 SKU 映射或订单明细；正式 SQL 仍需与订单和销售额样本对账。 |
| 状态 | 2026-06-26 三张表字段、主键、触发器、过程、事件、画像和订单抽取覆盖率已确认；2026-06-29 业务确认 `数量` 是销售个数，并确认 `wfproduct_management_latest` 中 `Related_Kit_Part` 为组合名、Component 行 `Supplier_Part_Number` 为子件名；WF CA / UK 派生规则、正式组合件 -> 子件销量分摊 SQL 待核实。 |

## K-WF-PRODUCT-001：Wayfair 产品管理最新表 / `wfproduct_management_latest`

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “Wayfair product_type 有几类”“Kit 和 Component 怎么对应”“组合件怎么展开到子件”“某 Supplier Part Number 的产品状态 / 分类是什么” |
| 业务目标 | 使用 `wfproduct_management_latest` 固定 Wayfair 产品管理 current、`Product_Type` 分类和组合件 -> 子件字段口径，并区分平台商品状态、Part / SPO 状态和公司采购 SKU 状态。 |
| 已知数据源 | `wfproduct_management_latest`，上游历史表为 `wfproduct_management`；current 由每日 09:50 `all_latest_ptcp_update -> wfproduct_management_latest_stg` 刷新，再由 `ptcpxq_product_stg` 写入 `平台产品详情表`。Part / SPO 另走 `wf产品管理 -> part状态最新_2 -> 需要创建spo的计划_2`。 |
| 核心字段 | `产品id`、`店铺`、`Supplier_Part_Number`、`SKU`、`Display_SKUs`、`Related_Kit_Part`、`Product_Type`、`Product_Status`、`Product_Class`、`Base_Cost`、`insert_date`、`min_insertdate`、`max_insertdate`、`extend`。 |
| 关键口径 | 物理主键为 `产品id`，唯一键为 `extend`。`Product_Type` 只有 4 类：`Standard`、`Kit`、`Sellable Component`、`Nonsellable Component`。`Kit` 是完整可售套组 SKU，由多个组件打包形成；`Sellable Component` 是可售组件，可随套组出售也可单独售卖；`Nonsellable Component` / True Component 是不可售组件，只能随套组出售；`Standard` 是标准独立产品 / Standalone Product，不属于任何套组组件，不能被拆入 `Kit`，仅支持完整单品独立销售。Component 行 `Related_Kit_Part` 为组合名字 / 组合件 part number，`Supplier_Part_Number` 为子件名称。 |
| 2026-06-29 画像 | 总行数 `19,773`，`Product_Type` 无空值：`Standard=10,242`、`Kit=5,456`、`Sellable Component=3,790`、`Nonsellable Component=285`。`Sellable Component` 中 `3,728` 行同店铺匹配产品管理表中的 `Kit` 行；`Nonsellable Component` 中 `205` 行匹配 `Kit`、`39` 行匹配到 `Standard`、`41` 行未匹配到产品管理表父级行。按业务定义，Component 指向 `Standard` 或未匹配父级应作为异常 / 待处理样本。 |
| 2026-07-01 画像 | 当前 `Product_Type` 合计 `22,253` 行：`Standard=12,722`、`Kit=5,456`、`Sellable Component=3,790`、`Nonsellable Component=285`；组件父级匹配结构与 2026-06-29 画像一致。 |
| 2026-07-15 生命周期复核 | current 23,630 行，其中 22,889 个最新有效产品键全部进入，另保留 741 个历史键。英文最新批次有 4,013 个重复产品键，current 过程用无序 `GROUP BY` 选取非聚合字段。439 个产品键只含空 Product Status，其中 231 个仍保留为平台详情 `上架` 且未标异常。CA / UK 当前有 593 个 Live 产品的链接 SKU 为字面值 `无`；平台详情另有 244 行平台 SKU 与 current 不一致。 |
| 状态与应用 | 商品状态查 `wfproduct_management_latest.Product_Status` / `平台产品详情表.上下架状态`；SPO Part 状态查 `part状态最新_2.status`；采购 SKU 公司状态查 `公司sku上下架表.sku状态`。当前 Part 链按小时进入 `需要创建spo的计划_2`，三类状态不可混用。 |
| 校验方式 | 按 `店铺 + Supplier_Part_Number` 查产品行；按 Component 行 `店铺 + Related_Kit_Part = 父级.店铺 + 父级.Supplier_Part_Number` 自连接，检查组合名字是否存在产品管理行，并统计 `Related_Kit_Part -> Supplier_Part_Number` 子件数量；父级优先应为 `Kit`，匹配到 `Standard` 时单独输出异常。 |
| 坑点 | 不能把 `Standard` 当作套组父级或组件处理；`Kit` 是套组整体。current 是 upsert 历史保留表，不等于仅含当前有效产品。`Not Live` 在平台详情当前仍映射为 `上架`；空状态可能保留历史上架且绕过异常标记。`part状态最新_2.max_date` 是最近状态变化时间，不是刷新时间。作为正式销量分摊 SQL 前，仍需和销售额、订单子件及采购 SKU 映射对账。 |
| 状态 | 表结构、生成过程、刷新事件、平台详情回填、Part / SPO 并行链和当前异常画像已确认；重复键确定性选行、空状态业务规则、CA / UK 缺链接和 09:50 并发顺序待核实。 |

## K-WF-SALES-001：Wayfair 上架 SKU 日销售额 / `总销售额1`

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “Wayfair 某 SKU / 店铺 / 月份总销售额是多少”“广告总销售额1 怎么回补”“为什么 CA/UK 总销售额和源表不一样” |
| 业务目标 | 用 `wf上架sku每日销售额` 作为 Wayfair 上架 SKU 日销售额和广告总销售额回补主源，避免混用未折算源表或错误利润字段。 |
| 已知数据源 | `wf上架sku每日销售额`；上游为 `wf_supplier_parts_performance`、`平台产品详情表`、`货币汇率`；FineBI 抽取数据集为 `zzdnb_wf上架sku每日销售额`。 |
| 核心字段 | `store`、`日期`、`supplier_part`、`currency`、`total_revenue`、`units_sold`、`链接sku`、`wayfair_sku`、`create_time`、`update_time`。`supplier_part` 是销售源 Supplier Part Number / 销售 SKU 层，不等同于订单拆分后的采购 SKU 或 `总账单详情_extract.上架sku` 子件层。 |
| 关键口径 | 同名事件每天调用同名过程；过程删除近 15 天 `create_time` 窗口后重写。`total_revenue` 写入时已对 `CAD/GBP` 按最新 `货币汇率` 折算，`USD` 基本与源表一致。2026-06-30 复核：本表 `supplier_part` 关联 `wfproduct_management_latest` 后可为 `Kit`、`Standard`、`Sellable Component` 或少量 `Nonsellable Component`；只有 `Kit` 或能通过 `Related_Kit_Part` 命中的父级才按组合件父层展开。 |
| 常用维度 | `store`、`LEFT(日期,7)`、`日期`、`supplier_part`、`wayfair_sku`、`链接sku`、`currency`。 |
| 默认过滤 | 按用户指定日期范围；广告总销售额回补优先按 `store + LEFT(日期,7) + wayfair_sku` 聚合。 |
| SQL 模板 | `SQL-AD-PROFIT-MERGE` 中的 `sales` CTE；查询时需 `SUM(CAST(NULLIF(total_revenue,'') AS DECIMAL(18,4)))`。 |
| 校验方式 | 检查目标表最新业务日期、事件最近执行时间、`store + 日期 + supplier_part + currency` 重复数、`total_revenue` 非数字、`链接sku/wayfair_sku` 缺失；按月与 `wf_supplier_parts_performance` 对比行数、销量和金额；需要分摊时再按 `store + supplier_part` 关联 `wfproduct_management_latest` 检查 Product_Type 和 `Related_Kit_Part`。 |
| 2026-07-01 复核 | 当前 `90,938` 行，业务日期 `2025-07-01` 至 `2026-06-28`，业务键重复数 `0`；2026 年 `store + supplier_part` 中 `Sellable Component=1,843`、`Kit=1,500`、`Standard=1,032`、`Nonsellable Component=38`、未匹配 `15`。 |
| 2026-07-01 分摊预检 | 新增 `SQL-WF-SALES-SUPPLIER-PART-EXPANSION-PRECHECK`。2026-06 `Kit` 有 `761` 个 `store + supplier_part`，其中 `487` 个有子件采购 SKU 映射；`Nonsellable Component` 有 `26` 个、未匹配 `12` 个，必须异常输出。该模板只做预检，不是已确认销售额分摊模板。 |
| 坑点 | 表无主键 / 索引；`total_revenue` 是字符串字段；`insert ignore` 没有唯一键时不是去重保障；CA/UK 与源表金额不同是汇率折算；接到 campaign / bid / 日明细后总销售额会重复铺行，汇总前必须去重或用平均聚合思路；若接 `总账单详情_extract`，不能用 `supplier_part = 上架sku` 直接关联；也不能把所有 `supplier_part` 都当 `Kit` 套组父件处理，`Sellable Component` 可能单卖也可能作为套组子件，少量 `Nonsellable Component` 销售源记录需单独异常输出；本表不提供已确认的总成本 / 总利润。 |
| 状态 | 2026-06-30 总销售额主链路、字段、事件、过程、源表对账和 Product_Type 分类画像已复核；成本 / 利润来源、汇率日期口径、补数窗口和正式分摊 SQL 仍待核实。 |

## K-OS-AD-001：Overstock 广告事实与采购 SKU 映射

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “OS 某店铺 / Campaign / SKU 的广告花费、销售额、ROAS、ACoS、CVR 是多少”“OS 广告怎么映射采购 SKU”“为什么 OS 广告按采购 SKU 汇总变大” |
| 业务目标 | 固定 OS 广告原始事实粒度、指标重算规则和订单 / 采购 SKU 展开边界，避免增强关联后重复累计广告指标。 |
| 已知数据源 | `sku_level_daily_advertising_rep`、`overstock_orders_allinfo`、`鲸汇店铺销售产品`；FineBI 数据集 `os广告重新计算`、`os广告2`；DDP 候选视图 `ddp汇总`。 |
| 核心字段 | 广告主键 `store + Campaign ID + Date + Short SKU`；广告维度 `Partner SKU/Campaign Name`；指标 `Clicks/Impressions/Spend/Units Sold/First Cost Sales/ROAS/ACoS/CVR`；订单键 `store + Retailer Order Number + Supplier SKU`；映射字段 `店铺SKU/采购SKU/数量/更新时间/insert_date/update_date`。 |
| 关键口径 | 广告表现默认先在原事实表聚合；`ROAS = SUM(First Cost Sales) / SUM(Spend)`，`ACoS = SUM(Spend) / SUM(First Cost Sales)`，`CVR = SUM(Units Sold) / SUM(Clicks)`。指标为文本字段，先转数值，NULL 按指标语义处理。 |
| 关联边界 | `os广告重新计算` 用 `Date + Short SKU + store` 回补订单，再按 `Supplier SKU + store + 生效期` 展开采购 SKU。该链路不是一对一；组合产品多采购 SKU 是业务展开，但广告指标不会自动分摊。 |
| 2026-07-10 校验 | 2026-06-01 至 2026-07-09，原广告 `11,843` 行，订单关联后投影 `14,060` 行；花费 `15,085.85 -> 24,102.84`，`First Cost Sales 163,492.74 -> 335,242.39`。2026-07-01 至 2026-07-09 非取消订单 `1,501` 行全部命中映射，其中 `126` 行命中多个采购 SKU。 |
| 默认选表 | 只问 OS 广告表现时查 `sku_level_daily_advertising_rep`；问订单或采购 SKU 时可参考 `os广告重新计算`，但必须先处理一对多；`os广告2` 当前使用状态和 DDP 唯一性仍待核实。 |
| 坑点 | 不平均行级 ROAS / ACoS / CVR；不在订单 / 采购 SKU 展开结果上直接求和广告指标；`OS-EZS` 只在映射层出现，是否属于有效广告范围待核实。 |
| 状态 | 广告事实粒度、字段、日期范围、三项比率公式和关联放大风险已确认；FineBI 组件实际选源、正式采购 SKU 分摊和 `ddp汇总` 候选键唯一性待核实。 |

## K-CUST-001：跨平台客诉 / 差评智能取数

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某店铺 / SKU / Model 的客诉率是多少”“哪些 SKU 客诉多”“差评率是多少”“为什么客诉率视图查不了” |
| 当前事实入口 | 客诉明细用 `all_ks_2`；差评明细用 `差评底表_2去重`；SKU / Model 映射用 `客诉sku_model映射表`。旧 `all_ks` 和旧 `wf_ks/os_ks/hd_ks` 不作为默认入口。 |
| 客诉事实粒度 | `store + 上架sku + po_number`，`extend` 唯一；`ks_num` 是事件数量，客诉率分子使用去重客诉订单数，不直接求和 `ks_num`。 |
| 客诉率口径 | `关联客诉率` 按下单月份统计客诉订单数；`当月客诉率` 按客诉产生月份统计客诉订单数；两者分母均为对应自然月同店铺 / 上架 SKU 的去重订单数。 |
| 差评率口径 | Wayfair 评分先除以 2，其他平台沿用原评分；当前实现把标准化评分 `<5` 计为差评并按 `website + reviewId` 去重。该阈值需业务确认。 |
| 刷新链路 | 每日 09:10 的 `客诉sku_model_cr` 事件补映射；每日 10:00 的 `每月客诉数量记录留存` 事件从 `all_ks_2` 写入两张月度留存快照。 |
| 2026-07-10 画像 | `all_ks_2` 为 `60,557` 行且 `extend` 唯一，覆盖 WF / HD / OS / AMZ；`5,384` 行缺 Model。差评去重底表 `10,806` 行，其中 4 星 `5,840` 行。 |
| 当前故障 | 五张客诉率 / 差评率汇总视图因 `utf8mb3_general_ci` 与 `utf8mb4_0900_ai_ci` 混用不可执行；修复前不作为智能问数入口。 |
| 取数边界 | 明细和计数可从可用底表受限查询；需要“率”时必须同时取得相同范围的去重订单分母，按分子分母重算，并标注当前视图故障和待核实口径。 |
| 状态 | 当前事实表、字段、粒度、刷新流程和视图故障已确认；差评阈值、缺 Model、未来日期和正式重算 SQL 待核实。 |

## K-LOG-001：订单包裹 / 柜运物流智能取数

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某订单物流到哪里了”“哪些包裹异常 / 退回”“某计划批次 / 柜号何时到港到仓”“某 SKU 在哪个柜” |
| 意图分流 | 问订单号 / 物流单号 / 物流商时查 `订单物流状态跟踪表`；问计划批次 / 柜号 / 提单 / 港口 / 入仓时查 `物流表` 或 `物流明细表`。两类事实不可混用。 |
| 订单事实粒度 | `订单号 + 物流单号`；订单数按去重订单号，包裹数按复合主键。多运单订单和共用运单必须单独保留。 |
| 订单状态字段 | 自动轨迹候选为 `物流详细信息状态`；`物流状态` 主要是插入时映射；`物流状态填报` 为手工状态。最终手工 / 自动优先级待核实，输出时可并列展示冲突。 |
| 柜运事实粒度 | `物流表` 一行一个计划批次 / 柜号；`物流明细表` 候选键为 `来源 + 计划批次号 + 柜号 + sku`，通过 `发货明细表` 展开 SKU，并合并指定外部入仓来源。该视图只包含人工船期。`入库单物流明细all(_2)` 则对开船 / 预计到港采用爬取优先、人工兜底，部分分支把爬取到港作为实际到港；问最新船期时优先回 `物流表` 或使用已确认日期优先级的入库视图。 |
| 入仓辅助 | `仓库入库单` 提供入库单、状态、预计到达、计划 / 实际入库数量；`柜号物流跟踪` 提供船东 + 提单级爬取船期。 |
| 默认安全边界 | 订单表会删除 90 天前已送达记录，不能用于完整历史妥投率；柜运人工日期存在大量未来异常和旧到仓值，当前状态需结合爬取字段和入库单状态。 |
| 2026-07-13 校验 | 订单物流 `67,246` 行、`51,276` 个订单；柜运 `物流表` `3,915` 行，`物流明细表` `33,007` 行且候选键无重复，缺 SKU `859` 行。 |
| 待核实 | FineBI 两个数据集是否直连上述物理对象；手工 / 自动订单状态优先级；柜运人工日期 / 爬取日期 / 入库单状态优先级；部分发货字段当前不可用。 |
| 状态 | 数据库对象、粒度、来源、事件和过程已确认；FineBI 展示口径及最终状态优先级待核实。 |

## K-REP-001：Wayfair 补件事实 / 补件成本 / 备件规划选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某订单申请了哪些补件”“补件原因 / 状态是什么”“某 SKU 某月补件费用多少”“某产品应该准备多少备件” |
| 意图分流 | 客户实际补件查 `wf_dropshiporders_replacementparts` 或补货记录视图；实际费用查 `store_bujian_sal`，月度平台 / 国家 / SKU 汇总查 `退货补件费用`；随货备件规划查 `补件sku表 + 补件数量`。 |
| 客户补件事实 | 原始主键为 `store + replacement order`，部件位于 `request_details` JSON；`wayfair订单补货记录底表` 和 `_2` 当前定义一致，并补充 PO、上架 SKU、Model、采购 SKU、客诉日期。 |
| 补件费用口径 | `valid_replacement_parts` 导入有效明细；`store_bujian_sal` 拆 SKU 并均摊成本；`退货补件费用.补件费用` 按平台、国家、SKU、月份汇总 `最终成本`。 |
| 备件规划口径 | `补件sku表` 是产品 SKU 到补件 SKU 的映射；`补件数量` 按入库 / 发货数量和预设比例估算应备数量，不代表实际客户申请或成本。 |
| 默认校验 | 补件事实检查一个 PO 多 replacement order 和视图重复；费用检查源最大业务日期、导入时间、平台归类和 SKU 映射覆盖；规划检查一对多补件 SKU 映射。 |
| 当前风险 | 补货记录视图按 `store + PO` 连接会放大；费用源在 2026-07-13 复核时只到 `2026-06-09`；Wayfair 费用明细仍有部分订单不能直接匹配后台 replacement order。 |
| 状态 | 三条选表链、成本生成公式和主要粒度风险已确认；FineBI 实际选源 / 去重、费用导入延迟原因和跨源订单号规则待核实。 |

## K-WF-INV-008：库存最终看板、虚拟库存与平台推送选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “库存最终看板为什么和平台推送不一致”“某 SKU 实际推了多少库存”“虚拟库存为什么还是旧活动”“订单为什么被推到某仓” |
| 意图分流 | 综合采购库存看 `库存最终看板`；Wayfair 库存提交看 `wf_submission库存变更记录`；HomeDepot 鲸汇库存计算 / 推送看 `鲸汇库存推送执行结果`；订单选仓过程看 `鲸汇推仓执行结果_python*`。 |
| 看板主链 | `warehouse_stock / 入库单物流明细all / 订货备料模块 / 销量模块 -> 系统库存在途模块 -> 库存最终看板 -> 库存最终看板记录留存`。看板不读取平台推送日志。 |
| 虚拟库存主链 | `warehouse_stock_最新 + 预计到仓 + 店铺产品映射 + 已销售量 + 推0清单 -> 活动提前调整虚拟库存_all_2 -> 库存推送sku虚拟库存设置`；后续库外推送器和读取优先级待核实。 |
| 订单推仓主链 | `鲸汇推仓仓库排序* + 鲸汇推仓特殊sku设置 -> 库外 Python 分仓 / 推仓 -> 鲸汇推仓执行结果_python*`。不能与库存数量推送混用。 |
| 新鲜度检查 | 看板查同步库存日和每日 23:55 留存；虚拟库存查原设置 `update_time / 开始时间 / 结束时间 / 原因`，不能只看历史表当前复制时间；推送日志查 `create_time`；订单推仓查各平台结果表 `create_time`。 |
| 当前风险 | WF 两类无结束时间虚拟规则最后更新停在 2026-04 / 2025-11；`库存借调分类` 双事件造成历史日每 SKU 两行，当前又受 `库存拆分记录` 停更影响；分仓占比骨架三个比例全部为空。 |
| 旧表边界 | `wf上架sku可用预估`、`wf上架sku可用预估2/3/4` 当前没有数据库生产者，不作为当前有货率入口；`系统库存在途模块_2` 未发现数据库消费者。 |
| 状态 | 数据库内三条分支、对象粒度和刷新已确认；库外推送器、WF 旧规则是否继续生效和 FineBI / 运营应用入口待核实。 |

## K-SALES-STORAGE-001：仓储费智能取数与利润边界

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某店铺 / 国家 / SKU 仓储费是多少”“Ship Out 90670 每日仓储费多少”“利润是否扣仓储费”“FineBI 扣除平均仓储费的利润率怎么来的” |
| 意图分流 | Wayfair 后台 Storage 账单查 `wf仓储费用明细`；Ship Out 90670 查 `ship_out_sku仓储费明细`；CG 理论每日费查 `cg仓每日仓储费`；魔方仓账单查 `mf_storage_fee_detail`；历史店铺月利润只参考 `仓储 -> 店铺月利润`。 |
| Wayfair 口径 | 固定 `charge_description='Storage'`，保留 `store / event_date / part_number / local_currency`，明确使用净额还是小计。映射采购 SKU 时按 Part + 生效期连接后，先按费用行 + 采购 SKU 去重。 |
| Ship Out 口径 | 行级 `仓储费 = 体积 * 仓储费率`；同一 `日期 + SKU + 仓库` 的库龄分段费用之和等于 `仓储费小计`。`体积` 已是总量，不能再乘库存数量。 |
| CG 计算口径 | `ROUND(费率 * 体积 * 该批次剩余库存 / 30, 4)`；费率受国家、包装类别和库龄影响。尺寸 / 分类缺失时输出异常，不用 0 补。 |
| 利润边界 | `上架sku利润表` 当前只从 Wayfair 费用表取 Fulfillment 发货费，不扣 Storage；历史 `店铺月利润` 会扣月度仓储汇总，但当前失效。FineBI 派生利润率的正式公式待核实。 |
| 新鲜度 | Wayfair 明细看 `event_date + create_time`；Ship Out 看文本日期解析后的最大业务日；魔方仓当前只到 2026-05；历史 `仓储` 只到 2025-08。事件 `LAST_EXECUTED` 不能代替结果检查。 |
| 坑点 | 费用类型混算、跨币种直接求和、负数调整被过滤、Ship Out 小计重复求和、采购 SKU 映射物理重复、缺尺寸按 0、把 Fulfillment 当 Storage。 |
| 状态 | 数据库对象、核心公式、历史利润扣减和主要重复风险已确认；FineBI 公式、外部生产者、正式下摊和当前利润选源待核实。 |

## K-WF-AD-MAP-001：Wayfair 广告到采购 SKU / DDP 的取数边界

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某条广告关联哪些采购 SKU”“按采购 SKU 看广告和 DDP”“广告 SKU 为什么拆成多行” |
| 事实入口 | 广告总量、花费、WSC、订单、件数、点击和曝光查 `advertising_product_report_by_day`；采购 SKU / DDP 标签查 `advertising_product_report_mapped`。 |
| 增强粒度 | 原始主键 `extend + isB2b` × `first_10_part_numbers` 拆分上架 SKU × 生效期采购 SKU 映射行。 |
| 不可加总规则 | 增强表完整复制原广告指标。没有正式分摊规则时，只能返回关联关系、覆盖率和源事实指标，不能直接按采购 SKU / Model 汇总。 |
| 映射检查 | 同时输出预期 Part 数、已映射 Part 数、未映射 Part 清单、每个 Part 的采购 SKU 数；当前最近 7 天 WF-TD / WF-FOP 未映射比例较高。 |
| DDP 检查 | DDP 按采购 SKU + 月份 + 店铺国家连接；先验证候选键唯一性，再输出空值。最近 7 天加拿大空值率为 `15.90%`。 |
| 新鲜度 | 先检查源表最大 `date_est`，再检查增强表最大 `date_est / etl_time`；事件每日 07:05，只重算近 30 天。 |
| 状态 | ETL、粒度、覆盖率和不可加总边界已确认；采购 SKU 正式分摊、未映射处理和实际下游待核实。 |

## K-AMZ-SALES-AFTER-001：Amazon 销售、退款、客诉与评论选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “Amazon 某 SKU 销量 / 销售额多少”“某订单是否退款”“某 SKU 退款多少”“退货 / feedback / replacement / 差评有哪些” |
| Listing 入口 | Listing 状态、价格、FBA 可售查 `领星erp亚马逊销售表 / amz上架产品管理_2`；它们不是订单交易事实。 |
| 销售入口 | 原始订单状态查 `amz_orders_allinfo`；标准化采购 SKU 销量 / 销售额查 `order_details_tb` 的 AMZ 分支，并先校验日期和非取消订单覆盖。 |
| MSKU 映射 | 查 `amz上架sku销售映射`，按 `store + MSKU` 返回全部采购 SKU 和数量；不能只取第一行。当前在售覆盖约 53%，未映射必须返回异常。 |
| 退款入口 | 原始平台售后字段查 `领星erp亚马逊售后订单`；采购 SKU 退款候选查 `amz_return_sal`，但需检查滞后，并按 `extend` 防止组合 SKU 金额重复。 |
| 月度红线 | `退货补件费用.平台=AMZ` 当前合并 Amazon + Walmart，且 AMZ 加拿大销售落入美国；修复前不作为纯 Amazon / 分国家正式月度入口。 |
| 客诉入口 | 退货、feedback、replacement 分别查对应底表；三类合并候选为 `amz_ks_2`。评论 / 差评单独查 `差评底表_2_amz`。 |
| 差评口径 | AMZ 当前按 1-3 星，即 `评级 <= 3`；不要套用通用链的 `<5`。月差评率视图当前不可执行。 |
| 默认校验 | 最大业务日期、订单覆盖、映射一对多 / 未命中、`extend` 退款重复、feedback / review 业务键重复、店铺标准化和排序规则错误。 |
| 状态 | 核心对象、ETL、粒度和主要风险已确认；库外订单装载、退款修复、月度口径和 FineBI 实际选源待核实。 |

## K-OS-INV-PRICE-001：Overstock 库存推送与上传价选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “OS 某产品当前基础成本是多少”“某日 / 未来促销上传价是多少”“某 SKU 是否推送库存、结果是什么” |
| 当前产品 / 成本 | 查 `os_manage_cost_latest`，粒度为 `store + supplier_sku`，产品 ID 是两者拼接。人工下架可能覆盖源上架状态。 |
| 按日上传价 | 查 `osprice`，粒度为 `产品id + date`；活动重叠取最低有效价，当天无活动用 `os_manage_cost.first_cost` 兜底，未来非促销没有兜底行。 |
| 上传价下游 | `link_sku_price_scj` 的 Overstock 分支从 `osprice` 取 `first_cost / promotional_first_cost`；09:30 和 14:30 两批，当前价查询需明确业务日期。 |
| 库存推送日志 | 查 `os库存变更记录`；它是一条条仓库级推送 / 检查事件，不是当前库存。`库存更改数量` 必须按数值、无变化、无映射分开。 |
| 映射规则 | 日志 `store` 空格改连字符后，以 `mysku = os_manage_cost_latest.supplier_sku` 关联；未命中必须输出，不能直接当作无库存。 |
| 新鲜度 | 产品 current 看 09:50 批次；上传价看 08:10 结果及业务 `date`；库存日志看最大 `create_time`。事件 `LAST_EXECUTED` 不能替代结果检查。 |
| 坑点 | 把上传价当前台价、把未来缺行当商品不存在、把库存日志当余额、混合字段强转数值、店铺空格 / 连字符导致漏匹配、误信失败清理事件。 |
| 状态 | 数据库选表、价格公式、调度和日志粒度已确认；外部采集 / 推送器、数值结果定义和清理修复待核实。 |

## K-INV-AGE-FIFO-001：库存库龄、剩余库存与店铺 FIFO 选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU / Part 各入库批次还剩多少”“库存库龄多少”“某店铺分到多少 CG / Shipout 库存”“为什么剩余库存为负” |
| 普通仓入口 | 批次余额查 `每天剩余库存拆分`，批次库龄查 `每天库存库龄拆分`；粒度均为 `日期 + 采购 SKU + 柜号 + 入库时间`。 |
| CG 入口 | 查对应 `_cg` 表；粒度增加 `store + part`，总可用只由 AMZ 和垂直两类组成。 |
| 店铺 FIFO | Shipout 查 `每天店铺库存数量_shipout`，CG 查 `_cg`；负剩余库存表示出库超过累计批次库存的欠货，不自动清零。 |
| 统一库龄 | 当前统一批次库龄查 `入库单库龄明细表_最新`，追源查 `入库单库龄明细_2`；Shipout 支路依赖库外 `ship_out库龄`。 |
| 计划 / 在途 | 业务实时值查视图 `计划发货和在途拆分`，稳定小时批次查 `_t`；字段已按其他仓 / CG、AMZ / 垂直、计划 / 在途拆成 8 类。 |
| 默认校验 | 最新日期、结果 / 上游批次、总可用加总、负库存、入库时间唯一键冲突、事件时间和目标表新鲜度。监控历史停采时不能只看 `LAST_EXECUTED`。 |
| 状态 | 核心过程、视图、粒度、公式、刷新和 2026-07-14 结果已确认；Shipout 库龄外部生产者、监控停采根因和 FIFO 索引是否调整待核实。 |

## K-INV-FORECAST-ALLOC-001：销量填报、每日预测与店铺分仓选表

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SKU 未来每日销量按什么算”“未来三个月各店铺怎么分库存”“为什么入库拆分比例不一致” |
| 月度填报 | 总量、垂直、AMZ 查对应 `销量预测填报表*`；店铺明细查 `销量预测填报表_各店铺`。先说明人工 / 系统值和平台分桶范围。 |
| 每日预测 | 普通 / AMZ / 垂直分别查 `每日销量预估`、`_amz`、`_垂直`；物理表 `销量预估` 是诊断结果，不是普通断货预测直接输入。 |
| 店铺分仓 | 查 `各店铺未来三个月销量占比*` 的 `店铺销量 / 总销量`；表名虽含“占比”，物理上保存的是分子和分母。 |
| 入库拆分 | Shipout / CG / CA 下游按比例 `CEIL` 初分并修正超额；总销量为 0 时按近 90 天分类销售额兜底。 |
| 默认校验 | 最新快照、同 SKU 分子和等于分母、总销量为 0 的兜底覆盖、CG `piaricco_CG`、CA `WF-FOP-CA`、填报操作人和总 / 子桶平台范围。 |
| 红线 | 不把 `各店铺的sku发各仓库的占比` 骨架当三个月店铺销量结果；不把总量、垂直、AMZ在不同平台 CASE 下强制相加；不按无序历史文本的位置解释月份。 |
| 状态 | 结构、公式、调度和直接消费者已确认；当日比例守恒、特殊店铺非零影响和各店铺填报生产者待核实。 |

## K-WF-SPO-001：Wayfair SPO 创建、目标仓与刷柜状态查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “哪些计划还没创建 SPO”“为什么没有生成 Plan”“为什么刷不到目标仓库”“能不能走备用仓”“是否需要重新刷柜” |
| 状态总览 | 批次分类优先查 `wfspo创建计划明细`；它覆盖全部 CG 入库物流历史。查当前待处理明细再落到 `需要创建spo的计划_2`，不要把两者行数直接对比。 |
| 当前计划入口 | `需要创建spo的计划_2` 主键为 `计划批次号 + sku + sku归属`。无 `Inbound_Order_ID` 表示尚未回填 SPO；是否可生成 Plan 还要整批检查 Part、Part 状态、运营确认、目标仓、计划日期和刷柜状态。 |
| Plan 输入 | 四店铺 `wf-*_shipment_2` 是批次头，`wf-*_shipment_items_2` 是 Part 明细。2026-07-15 当前 15 个候选批次与头表完全一致，明细无空 Part / 重复键。 |
| 目标仓 | `目标仓库表` 是配置，不是执行结果。`是否目标仓库 / 是否备选仓库` 决定候选仓列表；过程只发现新仓和计算船期差。 |
| 备用仓 | `备用仓库判定表` 按批次 + SKU 判断。WF-TD / WF-FOP 的美西转美东固定延迟 32 天；美西还依赖人工填报。只有整批所有 SKU 通过，计划表才写备用仓。 |
| Plan 与刷柜日志 | `创建plan日志` 是外部创建结果，`修改plan日志` 是外部仓库匹配结果，`wfspo刷柜记录` 是数据库整理后的成功 / 重刷结果。三者不是同一状态表。 |
| 重刷判断 | `cg仓spo是否重新刷柜` 当前按“新增 Part 不属于同 Wayfair SKU”或“发货总量上涨至少 19%”判断；目标 / 实际仓是否一致只展示。实际重刷申请与结果查 `重新刷仓记录表`。 |
| 默认校验 | 计划批次唯一性、整批 Part 完整性、shipment 候选一致性、目标 / 备用仓覆盖、日志批次号 / order_id 完整性、刷柜状态、结果新鲜度和历史重复。 |
| 坑点 | 当前 Plan 新日志成功 / 失败行没有批次号和 order_id，旧过程不能按批次消费；备用仓日更而计划每 2 小时更新；`计划批次号刷柜次数` 有 1,973 个历史双份批次；留存表是刷新前快照。 |
| 状态 | 数据库计划、配置、备用判断、shipment、日志、刷柜和重刷建议链已确认；外部 Plan 创建器、日志契约修复、重刷过程调用入口和生产调整待核实。 |

## K-WF-SPO-002：Wayfair SPO 入库与标签完成查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “计划对应哪个 SPO”“SPO 有哪些 Part / 数量”“标签是否全部下载”“SPO 到仓了吗”“为什么重刷没有基准量” |
| 计划到 SPO | 查 `cg仓计划批次号映射表`；只使用非空 `cg仓计划批次号`。当前非空映射是一对一，但物理复合主键允许未来出现多值，查询仍需做基数检查。 |
| SPO Part / 数量 | 查 `wayfaircg入库`，粒度为 Inbound Order + Part + order detail；计划、发运、收货、箱数和标签数均来自此表。 |
| 运输时间 | 查 `wayfaircg入库时间`，按 Supplier PO + Inbound Order + Part / shipment 取订舱、港口、预约和实际到仓时间；不要与 Part 数量事实混成一行后直接求和。 |
| 标签完成 | 应有 Part 来自 `wayfaircg入库`；成功 Part 来自标签日志中 error 为空且文件路径非空的记录。成功集合覆盖应有集合才算完成。 |
| 当前状态边界 | `下载spo标签填报表.是否已下载` 会误判；`共享盘cg_spo` 停在 2026-03-10；`ticket和spo创建.计划数='-'` 是缺基准，不是 0。 |
| 默认校验 | 映射一对一、映射到入库覆盖、应有 / 成功 Part 集合、错误日志、运输事实新鲜度、ticket 初始量可解析性。 |
| 状态 | 数据库消费链和当前影响已确认；四类库外写入器、调度、重试和修复方案待核实。 |

## K-WF-SPO-003：Wayfair CG 到仓、实际入库与店铺拆分查询

| 字段 | 内容 |
| --- | --- |
| 适用问题 | “某 SPO 到仓多少”“某 Part 实际入库多少”“AMZ / 垂直各分多少”“某店铺分到多少 CG 入库”“为什么店铺库存少一批” |
| 运输与计划量 | 运输节点和收货数量查 `wayfaircg入库时间`；SPO Part、计划数量查 `wayfaircg入库`。两表连接前必须检查 `store + Inbound Order + Part` 基数。 |
| 仓库实际入库 | 查 `仓库入库单` 且仓库以 `_CG` 结尾。`待入库` 可能已有部分实际量；当前实际到仓查询优先使用 `实际入库数量`，同时返回状态和入库时间。 |
| AMZ / 垂直拆分 | 查 `cg入库产品拆分_修改`，按 `柜号 + Part + 仓库名称` 汇总全部增量 / 修正行；不要只取最新行或过滤负数。 |
| 店铺拆分 | 查 `店铺库存拆分入库数量_cg`，按店铺汇总后必须与产品拆分累计实际量对账。2026-07-15 生产补写后普通覆盖完整，但仍有 5 个旧不守恒键和 56 个保存实际量陈旧键。 |
| 完整键边界 | 跨层对账用 `入库单号 + 柜号 + Part + 仓库名称`，店铺层再加 `store`。过程只按 `柜号 + Part` 判存在，当前已确认 14 个完整键、158 件被另一入库单历史记录误拦截。 |
| Part 到采购 SKU | 按 `wf更改part_number` 日期规则和 `cg_part_sku映射时间范围` 取全部命中；未命中可见过程回退去 `AWA-`，但正式问数仍要标映射来源。多命中不得任取。 |
| 非 CG 边界 | `产品入库明细拆分` 明确排除 `_CG`，不能用它回答 CG 入库拆分。 |
| 默认校验 | 只校验 `状态='已完成'` 的生产范围；检查完整业务键覆盖、店铺合计、保存实际量、跨入库单同名键、结果新鲜度和 `:20` 调度先后。FIFO 总量按店铺仓库 Part 汇总，零库存负批次单列。 |
| 状态 | 仓库到产品拆分主链和当前守恒已确认；店铺层剩余旧键、跨入库单误拦截、物理主键调整、同刻调度及零库存批次欠货口径待修复 / 核实。 |

# 存储过程到结果表映射

本文件记录存储过程、函数、触发器或类似数据库过程对结果表的影响。第一版仅整理已有文档线索，不连接数据库查询过程定义。

| 过程 / 函数 / 触发器 | 可能产出或影响 | 已知用途 | 已知来源 | 待核实项 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `wf上架sku每日销售额` | `wf上架sku每日销售额` / FineBI `zzdnb_wf上架sku每日销售额` | Wayfair SKU 日销售额、广告总销售额回补 | 2026-06-26 只读读取过程正文、事件元数据和表画像 | 成本 / 利润字段来源仍待核实；表无主键/索引，最新业务日期滞后需监控 | 已确认主链路，利润待核实 |
| `公司sku状态表_2` 相关过程 | `公司sku状态表` | SKU 状态、商品生命周期分析 | FineBI / 业务文档线索 | 过程名称、字段级逻辑、刷新时间 | 待核实 |
| `产品近两天的最小前台价` 相关过程 | `产品近两天的最小前台价` | 当前本品 / 竞品价格快照，支持 FineBI `本竞品价格对比` / `是否有价格优势` 线索 | 业务文档线索；2026-07-01 至 2026-07-02 只读元数据确认同名事件和价格相关过程存在 | 未读取正文；当前快照、上游价格源和已识别 temp 价表缺 `竞品` 行，需确认竞品抓取是否暂停、迁移或过程过滤变化 | 调度线索已确认，血缘待核实 |
| `wf_sku不断货销量` 相关过程 | `wf_sku不断货销量` | Wayfair 不断货销量或断货排除分析 | 待核实问题线索 | 生成逻辑、断货剔除口径、下游报表 | 高价值待核实 |
| `tb_kyxl_forecast_and_reality` 生成任务 | `tb_kyxl_forecast_and_reality`、合并 SKU / AMZ / 垂直版本 | 断货预测 | 2026-07-01 `information_schema` 元数据线索：`create_tb_kyxl_forecast_and_reality*`、`cursor_kyyg_create_tb_kyxl` 等过程/事件存在 | 未读取正文；字段公式、刷新补数机制、是否还有可拆仓上游 | 调度线索已确认，血缘待核实 |
| `总账单详情_extract_wayfair` / `总账单详情_extract_overstock` / `总账单详情_extract_homedepot` | `总账单详情_extract`、`总账单发货仓库映射`、`上架sku销售映射缺失记录` | 订单上架 SKU 拆到采购 SKU / 组件，用于销售、组合产品销量权重和缺映射排查 | 2026-06-26 只读读取过程正文和事件元数据；2026-06-29 用户确认 `数量` 是销售个数、`取消数量` 必须扣减 | 取消数量多组件扣减 SQL、过程副作用治理 | 已确认主链路，部分待核实 |
| `各平台上架sku销售映射` / 三平台同名过程 | `wayfair上架sku销售映射`、`overstock上架sku销售映射`、`homedepot上架sku销售映射` | 三平台 part number 到采购 SKU 映射，用于订单拆分、有货率和组合产品销量权重 | 2026-06-26 只读读取过程正文、事件元数据、触发器和表画像；2026-06-29 用户确认 `数量` 是销售个数 | WF `WF-TD-CA/UK` 从 `WF-TD` 派生规则、组合件 -> 子件 SQL | 已确认主链路，部分待核实 |
| 触发器 / 函数总数 | 待核实 | 需要全库结构学习时再统计 | 当前任务不连接数据库 | 数量、名称、影响表、业务风险 | 待核实 |

## 2026-07-10 客诉映射与月度留存过程

| 过程 | 已确认输入 | 已确认输出 / 影响 | 关键逻辑 | 状态 |
| --- | --- | --- | --- | --- |
| `客诉sku_model_cr` | `wf_ks_2`、`os_ks_2`、`hd_ks_2`、`amz_ks_2`、四平台订单表、`link_sku_table_2`、`amz上架产品管理_new` | `客诉sku_model映射表` | 插入尚不存在的 `platform + store + 上架sku + 产品id`；非 AMZ 从 `link_sku_table_2` 更新公司 SKU、Model、链接 SKU、类目，AMZ 从 `amz上架产品管理_new` 更新公司 SKU 和 Model。 | 已确认 |
| `每月客诉数量记录留存` | `all_ks_2` | `客诉产生月份_客诉数量记录`、`客诉下单月份_客诉数量记录` | 先删除当天快照，再分别按 `store + 客诉产生月份`、`store + 客诉下单月份` 写入去重 `po_number` 数。 | 已确认 |

- 2026-07-10 最新快照：产生月份表 `544` 行 / `18` 店铺，关联订单月份表 `551` 行 / `18` 店铺，留存日期均为 `2026-07-10`。
- `客诉sku_model映射表` 当前 `24,709` 行，`产品id` 与 `store + 上架sku` 均唯一，但 `4,902` 行 Model 为空；这表示过程已创建映射键，不代表上游一定能补齐业务属性。

## 2026-06-25 对象存在性更新

- 本轮通过 `information_schema` 只读元数据确认对象数量：存储过程 `279`、函数 `1`、触发器 `778`。
- 统计口径说明：这里的 `函数 = 1` 仅指 MySQL `ROUTINE_TYPE = 'FUNCTION'`；Navicat 左侧“函数”节点中显示为“过程”的对象属于 `PROCEDURE`，已计入 `存储过程 = 279`。
- 本轮未读取存储过程正文、函数正文或触发器正文。
- 因此本轮不能确认输入表、输出表、字段级逻辑或真实影响范围。
- 详细对象清单见 `reports/固定流程取数和报表/SUPPORT_数据库对象地图/database_catalog_2026-06-25.xlsx` 的 `对象清单` 页。
- 后续如需确认血缘，应按业务主题选择少量高价值过程或触发器，再在获得明确允许后读取定义或通过 FineBI / 任务日志交叉验证。
- 状态：对象存在已记录，血缘关系全部 `待核实`。

## 2026-07-02 价格优势过程线索

- 本轮只读确认价格相关过程 / 事件名称存在，包括 `link_sku_price_qtj_fabric`、`link_sku_price_scj`、`ptcpxq_update_qtj_scj_price`、`产品近两天的最小前台价` 等。
- 2026-07-02 后续补充：已只读读取相关过程定义关键词和少量片段，不输出过程全文。`link_sku_price_qtj_fabric` 过程仍有竞品分支，引用 `product_fabric`、`wayfair_product_pricedownload`、`link_sku_table_2`；`产品近两天的最小前台价` 从 `link_sku_price_qtj_fabric` 取最近两天并按 `产品类型` 聚合，本身未见显式排除竞品。
- 已识别候选表中，`temp_link_sku_price_qtj_fabric`、`temp_latest_link_sku_price_scj` 当前回连后也未见可用竞品价格；`竞品对标表` 只有关系，`本竞品失效sku` 只有状态。
- 2026-07-02 用户确认：本次竞品价未写入 `link_sku_price_qtj_fabric` / `产品近两天的最小前台价` 的原因是相关函数/例程执行失败，现已修复；恢复后的竞品行数量、最新日期和样本对账待只读复核。
- 2026-07-02 修复后复核：下游 `产品近两天的最小前台价` 已恢复竞品行，`create_time=2026-07-02 06:40:00`，`竞品 4,649` 行；上游 `link_sku_price_qtj_fabric` 因大表查询超时仍待补充。
- 样本线索：最新竞品映射样本可在 `product_fabric` 近 90 天命中部分价格，但当前目标表仍只见 `本品`。
- 状态：过程链路方向已样本验证，竞品价未落表根因仍待核实；后续需复核过程竞品分支写入条件、主键冲突 / `insert ignore`、清表重建和事件覆盖顺序，或结合 FineBI 当前抽取样本验证。

## 2026-06-26 `wf上架sku每日销售额` 过程 / 事件确认

- 目标表：`wf上架sku每日销售额`；FineBI 抽取数据集名为 `公共数据/王威/广告/zzdnb_wf上架sku每日销售额`。
- 直接触发器：未发现。
- 定时事件：`wf上架sku每日销售额`，`ENABLED`，每 `1 DAY`，从 `2026-04-23 08:27:00` 开始；2026-06-26 查询时最近执行 `2026-06-26 08:27:00`。
- 过程主逻辑：
  - 删除目标表中 `create_time >= CURRENT_DATE - 15 day` 的数据。
  - 从 `wf_supplier_parts_performance` 读取 `store`、`日期`、`supplier_part`、`currency`、`total_revenue`、`units_sold`、`create_time`、`update_time`。
  - 左连 `平台产品详情表`，按 `store + supplier_part = store + 上架sku` 回补 `链接sku` 和 `平台sku as wayfair_sku`。
  - 左连最新日期的 `货币汇率`，对 `currency='GBP'` 映射 `英镑`、`currency='CAD'` 映射 `加拿大元` 后折算 `total_revenue`；`USD` 或无汇率时乘 `1`。
  - 仅重写源表 `create_time >= CURRENT_DATE - 15 day` 的窗口数据。
- 层级说明：上述 `平台产品详情表.上架sku` 是销售额过程用于回补 `链接sku/wayfair_sku` 的商品维表字段；用户 2026-06-26 业务确认的红线是不要把 `wf上架sku每日销售额.supplier_part` 直接等值关联到 `总账单详情_extract.上架sku`，后者在 WF 订单中是子件 part number。
- 表结构风险：目标表无主键、无二级索引；过程使用 `insert ignore`，但没有唯一键时不能作为业务去重保障。2026-06-26 当前按 `store + 日期 + supplier_part + currency` 校验未发现重复。
- 数据滞后线索：事件最近执行为 `2026-06-26 08:27:00`，但目标表与源表最新业务日期均为 `2026-06-23`。
- 下游影响：`WF广告数据/广告` 的 `销售额` / `总销售额1`、WSP 广告组月报总销售额回补、广告销售占比。
- 待核实：成本 / 总利润字段来源、汇率表是否应使用“最新汇率”还是业务日期汇率、无索引对大范围查询的性能影响。

### 2026-06-30 补充复核

- 过程正文、字段和写入逻辑未变化；事件最近执行为 `2026-06-30 08:27:00`。
- 当前目标表 `90,322` 行，业务日期覆盖 `2025-07-01` 至 `2026-06-27`；源表 `wf_supplier_parts_performance` 最新业务日期同为 `2026-06-27`。
- 按 `store + 日期 + supplier_part + currency` 未发现重复；`total_revenue` 空值 / 非数字为 `0`，`units_sold` 空值为 `0`。
- 2026-06 按店铺/月与源表对账：行数和销量一致；`WF-FOP/WF-TD` 金额一致；`WF-TD-CA/WF-TD-UK` 金额差异延续汇率折算口径。
- 口径补充：目标表 `supplier_part` 是销售源 Supplier Part Number / 销售 SKU 层。关联 `wfproduct_management_latest` 后可分为 `Sellable Component`、`Kit`、`Standard`、少量 `Nonsellable Component` 和未匹配值；正式组合件分摊 SQL 需要先识别 Product_Type，再用 `Related_Kit_Part` 展开父子关系，不能把所有 `supplier_part` 机械当作 `Kit` 父件。

## 2026-06-26 `总账单详情_extract` 过程 / 事件 / 触发器确认

- 目标表：`总账单详情_extract`。
- 直接触发器：
  - `zzdxq_insert_copy2_copy1`：`BEFORE INSERT`，写入 `new.create_time=now()`。
  - `zzdxq_update_copy2_copy1`：`BEFORE UPDATE`，写入 `new.update_time=now()`。
- 定时事件：
  - `总账单详情_extract_wayfair`：`ENABLED`，每 `1 HOUR`，从 `2026-04-22 14:06:00` 开始；2026-06-26 查询时最近执行 `2026-06-26 15:06:00`。
  - `总账单详情_extract_overstock`：`ENABLED`，每 `2 HOUR`，从 `2026-06-17 11:08:00` 开始；最近执行 `2026-06-26 15:08:00`。
  - `总账单详情_extract_homedepot`：`ENABLED`，每 `2 HOUR`，从 `2026-06-17 11:10:00` 开始；最近执行 `2026-06-26 15:10:00`。
- 过程主逻辑：
  - Wayfair：从 `wayfair_orders_allinfo`、`wayfair_orders_allinfo_api`、`鲸汇wayfair订单管理` 取订单，关联 `wayfair上架sku销售映射` 拆到采购 SKU；`wayfair_orders_allinfo` 从 `2026-04-01` 起补，API / 鲸汇默认最近 `@adjust_extract_days=10` 天补；过滤 `MultiChannelAPI`、`MultiChannelManualEntry` 和 `PO Number like '%-R-%'`；用 `NOT EXISTS` 避免重复插入。
  - Overstock：从 `overstock_orders_allinfo` 取 `2026-06-01` 起订单，关联 `overstock上架sku销售映射`；同时回填运输方式、Tracking、订单状态和取消数量。
  - Homedepot：从 `homedepot_orders_allinfo` 取 `2026-06-01` 起订单，关联 `homedepot上架sku销售映射`；同时回填订单状态。
- 共同回填：
  - 从 `公司sku上下架表` 回填 `category`。
  - 从映射表或 `wfproduct_management_latest` 回填平台 SKU / 平台分类。
  - 将 `总账单详情_extract` 中的 `出单店铺 + 发货仓库细分` 插入 `总账单发货仓库映射`，再回填 `发货仓库`。
  - 清理并写入 `上架sku销售映射缺失记录`，用于记录找不到采购 SKU 映射的订单上架 SKU。
- 函数：2026-06-26 当次扫描仅见 MySQL `FUNCTION` `normalize_simple_parts`，且未引用 `总账单详情_extract`；当前函数清单以文末 2026-07-13 更正为准。
- 重要副作用：`总账单详情_extract_wayfair` 会更新 `鲸汇wayfair订单管理.shop_name` 的部分店铺名标准化值；如只想刷新 `总账单详情_extract`，需要意识到该过程并非只写目标表。
- 2026-06-29 业务口径补充：`数量` 是销售个数；`取消数量` 必须扣减。
- 待核实：过程是否由业务正式维护为当前口径、取消数量在多组件行的 SQL 扣减实现、过程副作用治理。

## 2026-06-26 三平台上架 SKU 销售映射过程 / 事件 / 触发器确认

- 目标表：`wayfair上架sku销售映射`、`overstock上架sku销售映射`、`homedepot上架sku销售映射`。
- 总过程：`各平台上架sku销售映射()`，依次调用三平台同名映射过程。
- 定时事件：`各平台上架sku销售映射`，`ENABLED`，每 `1 DAY`，从 `2025-05-19 23:00:00` 开始；2026-06-26 查询时最近执行 `2026-06-25 23:00:00`。
- 三张表共同触发器：
  - `BEFORE INSERT`：写入 `new.create_time=now()`。
  - `BEFORE UPDATE`：写入 `new.update_time=now()`。
- 共同表结构：主键均为 `store + part_number + 采购sku`；字段均包含 `store`、`part_number`、`product_class`、`product_type`、`product_sku`、`product_status`、`base_cost`、`上传价`、`采购sku`、`数量`、`ddp价格`、`来源`、`update_time`、`create_time`。
- 共同刷新逻辑：删除本表 `来源='鲸汇店铺销售产品最新'` 的旧数据，从 `鲸汇店铺销售产品最新` 重新插入，按 `store + part_number + 采购sku` 排重，再从 `产品最新ddp值` 回补 `ddp价格`。
- Wayfair 特殊逻辑：过程直接取 `WF-TD`、`WF-FOP`，并把 `WF-TD` 映射复制派生为 `WF-TD-CA`、`WF-TD-UK`；随后按 `store + part_number` 从 `wfproduct_management_latest` 回补产品分类、SKU、状态和成本字段。
- Overstock 特殊逻辑：只取 `OS-FOP`、`OS-LJD`、`OS-SP`。
- Homedepot 特殊逻辑：只取 `HD-1JAY`、`HD-FOP`、`HD-GMI`、`HD-LJD`、`HD-SSL`。
- 与订单抽取关系：`总账单详情_extract_wayfair` / `overstock` / `homedepot` 过程通过对应映射表把订单 part number 拆到采购 SKU；缺映射会进入 `上架sku销售映射缺失记录`。
- 2026-06-29 业务口径补充：映射表 `数量` 是销售个数。
- 待核实：WF CA / UK 复制 TD 映射是否为正式业务规则；Wayfair 组合件 -> 子件展开到 `wf上架sku每日销售额` 的正式分摊 SQL、去重和异常输出方式。

## 2026-07-01 库存预测 / 拆分过程线索

本轮只查询 `information_schema.ROUTINES` 元数据，未读取过程正文，以下仅为可能映射关系。

| 过程 | 可能产出或影响 | 已知用途线索 | 待核实项 | 状态 |
| --- | --- | --- | --- | --- |
| `create_tb_kyxl_forecast_and_reality` | `tb_kyxl_forecast_and_reality` | 采购 SKU 预测事实表 | 输入表、字段公式、是否清表重建、补数窗口 | 待核实线索 |
| `create_tb_kyxl_forecast_and_reality_合并` | `tb_kyxl_forecast_and_reality_合并sku` | 合并 SKU 预测事实表 | 合并规则、与普通 SKU 的映射和字段公式 | 待核实线索 |
| `create_tb_kyxl_forecast_and_reality_amz` | `tb_kyxl_forecast_and_reality_amz` | AMZ 预测分支 | AMZ 过滤条件和字段公式 | 待核实线索 |
| `create_tb_kyxl_forecast_and_reality_垂直` | `tb_kyxl_forecast_and_reality_垂直` | 垂直预测分支 | 垂直过滤条件和字段公式 | 待核实线索 |
| `cursor_kyyg_create_tb_kyxl` | 预测事实表批量生成链路 | 同名事件每日执行 | 是否调用上述过程、执行顺序、失败补数机制 | 待核实线索 |
| `断货sku数量_新增` / `断货sku数量_新增_合并` | `断货sku数量` / `断货sku数量_合并` | 月度断货 KPI | KPI 公式、是否从 `各sku每月断货天数` 或事实表聚合 | 待核实线索 |
| `库存拆分记录保存` | `库存拆分记录` | 每小时保存拆分结果 | 拆分公式、手动调整来源、负库存处理 | 待核实线索 |
| `cg仓库库存拆分` / `cg各仓库库存拆分` / `cg仓店铺库存拆分总` | CG 库存拆分相关表 | CG 仓库存拆分 | 90670 源仓、借调、AMZ/垂直拆分公式 | 待核实线索 |
| `shipout库存拆分` / `shipout每天库存` / `shipout店铺库存拆分总` | `shipout每天库存` 等 | shipout 库存拆分 | 最新日期停滞和负库存原因 | 待核实线索 |
| `生成仓库每日销量断货情况_表` 及 AMZ / 垂直版本 | `仓库每日销量断货情况_表` 等 | 售罄预估基础结果表 | `是否断货`、可用、在途、销量字段来源 | 待核实线索 |

注意：上述名称匹配很强，但未读取过程正文，不能当成已确认血缘；下一轮如果要升级为已确认，应只选少量核心过程读取定义并记录输入输出。

## 2026-07-06 库存 / 预测 / 拆分过程写入目标补充

本轮通过 `scripts/learn-inventory-forecast-lineage.js` 只读提取相关过程的调用目标、写入目标和引用对象，不保存过程正文全文。以下为元数据和定义摘要提取结果；字段公式仍需后续按过程片段或样本对账确认。

| 过程 | 写入目标 / 调用摘要 | 状态 |
| --- | --- | --- |
| `cursor_kyyg_create_tb_kyxl` | 调用 `生成仓库每日销量断货情况_表`、`生成warehouse_stock_在售`、`create_tb_kyxl_forecast_and_reality`、`cursor_kyyg_2`、`cursor_xnyg`、`update_kyygbjjx`。 | 调用链已样本验证 |
| `create_tb_kyxl_forecast_and_reality` | 写入 `tb_kyxl_forecast_and_reality`，并调用 `create_tb_kyxl_forecast_and_reality_合并`。 | 写入目标已确认 |
| `create_tb_kyxl_forecast_and_reality_合并` | 引用 `tb_kyxl_forecast_and_reality_合并sku`、`预估售罄合并sku列表` 等对象；本轮自动提取未稳定识别写入语句。 | 待公式级复核 |
| `create_tb_kyxl_forecast_and_reality_amz` | 写入 `tb_kyxl_forecast_and_reality_amz`。 | 写入目标已确认 |
| `create_tb_kyxl_forecast_and_reality_垂直` | 写入 `tb_kyxl_forecast_and_reality_垂直`。 | 写入目标已确认 |
| `断货sku数量_新增` | 写入 `断货sku数量`，并调用 `断货sku数量_新增_合并`、`考虑放弃sku清单更新品类`。 | 写入目标已确认，公式待核实 |
| `断货sku数量_新增_合并` | 写入 `断货sku数量_合并`，并调用 `考虑放弃sku清单更新品类`。 | 写入目标已确认，公式待核实 |
| `库存拆分总` / `库存拆分总_cg` | 总控过程，调用 `订单出库数量`、每天出库 / 剩余库存 / 库龄拆分、`cg仓库库存拆分`、`cg各仓库库存拆分`、`shipout每天库存` 等子过程。 | 调用链已样本验证 |
| `库存拆分记录保存` | 写入 `tmp_库存拆分` 和 `库存拆分记录`，引用 `产品库存拆分`。 | 写入目标已确认；字段公式补充见下一节 |
| `cg仓库库存拆分` | 写入 CG 入库 / 出库拆分中间对象，如 `cg入库产品拆分`、`cg出库订单`。 | 写入目标已样本验证 |
| `cg各仓库库存拆分` | 写入 `cg仓每天库存`、`cg仓库库存借调`、`cg仓库借调库存详情`、`cg仓转库库存` 等。 | 写入目标已样本验证 |
| `shipout库存拆分` | 写入 `shipout每天库存`、`shipout库存借调`、`shipout每天出库数量` 等。 | 写入目标已样本验证 |
| `预估售罄排除sku新增` | 写入 `预估售罄排除sku`，引用 `warehouse_stock`、`warehouse_stock_最新`、`公司sku上下架表`。 | 写入目标已样本验证 |
| `生成仓库每日销量断货情况_表` | 写入 `仓库每日销量断货情况_表` 和 `仓库每日销量断货情况_表_新品变化`。 | 写入目标已确认 |
| `生成仓库每日销量断货情况_表_amz` | 写入 `仓库每日销量断货情况_表_amz`。 | 写入目标已确认 |
| `生成仓库每日销量断货情况_表_垂直` | 写入 `仓库每日销量断货情况_表_垂直`。 | 写入目标已确认 |

修正线索：事件 `库存拆分` / `库存拆分_cg` 存在，但数据库未发现同名过程；实际被事件调用的是 `库存拆分总` / `库存拆分总_cg`。后续写血缘或排查任务时应使用实际过程名。

## 2026-07-06 库存拆分字段公式补充

本轮通过一次性只读查询继续提取库存拆分公式线索，不保留 `scripts/` 下固定脚本，不重复跑对象基线和事件调用链，不保存完整过程 / 视图定义。

| 对象 / 过程 | 血缘或公式线索 | 状态 |
| --- | --- | --- |
| `库存拆分记录保存` | 从视图 `产品库存拆分` 取 `sku`、`系统`、`总可用库存`、`其他仓库可用库存`、`cg仓可用库存`、AMZ / 垂直拆分字段和 `手动调整库存`，先写会话临时表 `tmp_库存拆分`，再插入 `库存拆分记录`。 | 写入方式已确认；业务口径部分确认 |
| `产品库存拆分` | 视图引用 `warehouse_stock`、`产品入库明细拆分`、`订单出库数量`、`每天剩余库存拆分新_cg`、`ship_out产品库存变更记录`、`库存拆分增减异常表` 等对象。 | 来源对象已提取；别名级逐项解释待补 |
| `库存拆分记录` | 最新日 `2026-07-06` 样本验证：`总可用库存 = 其他仓库可用库存 + cg仓可用库存`、`amz可用库存 = 其他amz可用 + cgamz可用`、`垂直可用库存 = 其他垂直可用 + cg垂直可用` 均 `0` mismatch。 | 样本验证 |
| `cg各仓库库存拆分` | 片段显示会在 AMZ 或垂直可用为负、且两者合计可覆盖时写入 `cg仓库库存借调`；并写入 `cg仓库借调库存详情`。 | 借调触发线索已确认；业务解释待核实 |
| `shipout库存拆分` / `shipout每天库存` | 片段显示类似 AMZ / 垂直借调逻辑；`shipout` 出库计算中出现 `90670 Warehouse` 过滤。 | 90670 和借调影响待业务确认 |

## 2026-07-13 订单物流 / 柜运物流过程补充

| 过程 | 读取 / 写入对象 | 已确认逻辑 | 状态 |
| --- | --- | --- | --- |
| `订单物流状态跟踪表` | 读取 `ship_out出库_已完成`、`鲸汇自发货订单all_cf`、`wayfair_orders_allinfo`；写入 / 更新 / 清理 `订单物流状态跟踪表` | 拆分多物流单号，插入订单 + 运单事实；按第三方查询状态和轨迹 JSON 更新 `物流详细信息状态`；清理 90 天前已送达记录 | 已确认；最终状态优先级待核实 |
| `物流表cg入库单号更新` | 读取 `cg仓计划批次号映射表`、`仓库入库单`、`交期跟进表`、`mask_booking`；更新 `物流表` | 回填 CG 入库单号、是否产生入库单、工厂交期和订舱线索 | 已确认 |
| `物流表相关爬取物流时间更新` | 读取 `柜号物流跟踪`；更新 `物流表` | 按 `船东 + 提单号` 回填 `开船时间_爬取/预计到港日期_爬取/到港日期_爬取` | 已确认 |
| `柜号物流跟踪状态更新` | 读取 `入库单物流明细all`；更新 `柜号物流跟踪` | 按计划批次号同步入库 / 物流状态 | 已确认 |

- 本轮只读读取以上过程定义，没有调用过程或执行数据库写操作。
- `物流明细表` 是视图：`物流表 LEFT JOIN 发货明细表 ON 柜号`，再合并指定 `other_warehousingentry_stg` 来源；这条视图血缘已确认。

## 2026-07-13 补件成本 / 备件规划过程补充

| 过程 | 读取 / 写入对象 | 已确认逻辑 | 状态 |
| --- | --- | --- | --- |
| `store_bujian_sal` | 读取 `valid_replacement_parts`；增量写入 `store_bujian_sal` | 对未处理来源 id 递归拆分多 SKU 货号；`最终成本 = 补件成本 / 拆分 SKU 数` | 已确认 |
| `退货补件费用` | 读取 `order_details_tb`、各平台退货 / 退款表、`store_bujian_sal`、`公司sku状态表`；重建 `退货补件费用` | 建立 SKU × 平台 × 国家 × 月份网格，聚合销量、销售额、退货金额和 `SUM(最终成本)` | 已确认；最终利润使用待核实 |
| `补件sku表更新` | 读取 `补件sku汇总表2025年_stg`；重建 `补件sku表` | 按 `产品SKU + 补件SKU` 生成备件映射 | 已确认 |

- 客户 replacement 事实另由视图 `wayfair订单补货记录底表(_2)` 从 `wf_ks_2 + wf_dropshiporders_replacementparts + sku_model映射表` 组合，不属于上述补件成本过程。
- 本轮只读读取过程定义，没有调用过程。成本源最大业务日期停在 `2026-06-09`，事件成功与源数据完整性必须分别判断。

## 2026-07-13 库存预测 / 拆分 ETL 收口

本轮只读查看选定过程、函数、视图和事件定义，没有调用过程或修改数据库。以下记录替代“仅对象名线索”，但不保存完整定义正文。

| 调度 / 过程 | 主要输入 | 主要输出 / 行为 | 状态 |
| --- | --- | --- | --- |
| `cursor_kyyg_create_tb_kyxl` | `warehouse_stock`、在售状态、每日销量、预计到仓与预测相关对象 | 每日 04:05 依次生成在售库存、历史断货、每日可用预估、普通预测及合并 SKU 预测 | 主调用链已确认 |
| `生成warehouse_stock_在售` | `warehouse_stock`、`公司sku上下架表` | 增量写 `warehouse_stock_在售`，并清理日期和排除分类 | 已确认 |
| `生成仓库每日销量断货情况_表` | `每日各仓库销量表_表`、`warehouse_stock_在售`、`仓库映射表`、订单 / Model 映射 | 重建 `仓库每日销量断货情况_表`；`SUM(可用)=0` 才标断货 | 已确认；负可用产生 NULL 风险 |
| `cursor_kyyg_2` | 视图 `各sku每日到仓及销量视图`、`warehouse_stock_最新` | 重建 `t_ygb` 和 `各sku仓库每日可用预估`；未来日结余不小于 0 | 已确认 |
| `create_tb_kyxl_forecast_and_reality` | 未来每日可用预估、历史销量断货事实 | 写普通预测事实并调用合并 SKU 分支 | 已确认 |
| AMZ / 垂直预测总控 | 对应 AMZ / 垂直库存、销量和预测对象 | 每日 08:40 / 08:50 写对应预测事实 | 已确认 |
| `断货sku数量_新增` | 普通 / 合并预测事实、`考虑放弃sku清单` | 每日 09:40 写月度断货 KPI；断货日按 `可用=0 OR 预估可用=0` | 已确认 |
| `库存拆分总` | 普通仓和 Shipout 出入库 / 库存对象 | 每小时 :50 运行订单出库、过程 `shipout每天库存` 及普通仓剩余库存 / 库龄拆分 | 已确认；当前写 `_2` 系列 |
| `库存拆分总_cg` | CG 入库、出库、剩余库存和借调对象 | 每小时 :19 运行 CG 拆分，并调用 `cg各仓库库存拆分` | 已确认 |
| `库存拆分记录保存` | 视图 `产品库存拆分` | 每小时 :30 删除当日后直接插入 `库存拆分记录`，无去重 | 已确认；当前因来源同 SKU 重复触发主键冲突 |

### 视图和函数边界

- `产品库存拆分` 直接读取基础表，不是多层视图嵌套；已确认 `总可用库存=其他仓库可用库存+cg仓可用库存`，AMZ / 垂直字段分别由其他仓和 CG 分量相加。
- 当前过程 `shipout每天库存` 写入 `shipout每天库存_2` 等 `_2` 对象；旧过程 `shipout库存拆分` 才写旧 `shipout每天库存`。智能取数必须先看目标表新鲜度，不能按过程名猜表名。
- 库存 ETL 未调用 MySQL `FUNCTION`。`normalize_simple_parts(input, delim)` 仅由价格过程 `link_sku_price_qtj_fabric` 调用；当前另有 3 个 WFCG 函数属于利润发货费链，见文末补充。
- 所选结果表上的触发器均只维护 `create_time` / `update_time`，不承载库存或预测公式。

## 2026-07-13 DDP / 利润结果链收口

| 调度 / 过程 | 主要输入 | 输出与公式 | 当前边界 |
| --- | --- | --- | --- |
| `ddp汇总表` | 视图 `ddp汇总`；其上游为 `预计ddp`、`实际ddp`、日期、SKU 状态、SKU 修正和 DDP 价格记录 | 每日 06:46 重建 `ddp汇总表_美国` | 过程里的 `ddp_integration` 是旧注释 / CTE 别名，不是物理表 |
| `ddp汇总表_加拿大` / `_英国` | 对应国家预计 / 实际 DDP、SKU 状态、价格记录 | 每 2 小时 :30 重建国家表；实际优先、预计其次、价格记录兜底，再沿用最近有效值 | 当前 `sku + 年月` 无重复；表无物理主键 |
| `上架sku利润表` | 当前上架产品、汇率、三国 DDP、产品 / CG 发货费、公司 SKU 状态和销量辅助 | 每日 12:42 删除当日后重建；`成本=发货费+ddp`，平台佣金单列，另算标签和 `利润率上限` | 是当前成本快照，不是订单利润；HD / 英国佣金覆盖不完整 |
| `钉钉利润提醒表` | `order_details_tb_profit`、三国 DDP、产品 / CG / 魔方仓发货费、公司 SKU 状态 | `总利润=平台折后销售额-发货费-DDP`；仅增量写入近 30 天亏损且非停止采购的 PO | 不删除历史、无订单日期；未发现数据库事件 / 调用者，外部调度待核实 |
| `退货补件费用` | 平台退货源、`store_bujian_sal`、订单销售和公司 SKU 状态 | 每日 08:12 重建平台 + 国家 + SKU + 月份退货补件结果 | 已确认，但尚未进入上述两个订单 / 成本结果过程 |

### 失效视图说明

- `店铺月利润` 不是过程结果，而是 Wayfair 美国店的月度经营净利润视图。公式还扣广告、补件、测评、平台费用和仓储费用，与 `ddp_利润查询` 的订单利润口径不同。
- 当前主回款源最大票据日期为 2025-06-10，已经落在视图最近 9 个月窗口之外；`wf账单扣款.Payment Amount` 也为全空，旧补件费用仍按每单固定 5。该视图当前返回 0 行，应视为历史 / 失效入口。

## 2026-07-13 库存 / 利润 / 物流 ETL 增量复核

| 过程 / 视图 | 输入 | 输出 / 关键逻辑 | 结论 |
| --- | --- | --- | --- |
| `生成warehouse_stock_最新` | `warehouse_stock`、`仓库映射表`、`公司sku上下架表` | 按非停用仓库分别取源表最大日期，全量重建 `warehouse_stock_最新`；SKU 转大写并回补一 / 二级分类；同批追加到 `warehouse_stock_最新_temp` 并删除 12 小时前缓冲 | 生成逻辑已确认；正式当前快照查主表，不查多批次缓冲表 |
| `产品发货费` | `ship_out产品管理`、`出仓费` | 按重量区间和特殊 SKU / Light 规则计算 `出货费用`，最后按 SKU 分组 | 当前 4,971 个 SKU 有多仓源行、49 个 SKU 重量值不同，但计算出货费用均无冲突；费用当前可复用，仓库 / 重量展示列不能当确定性单行来源 |
| `物流表cg入库单号更新` | `cg仓计划批次号映射表`、`仓库入库单`、`交期跟进表`、`mask_booking` | 每 5 分钟回填 CG 入库单号、入库单产生状态、工厂交期和订舱状态 | 过程定义和当前写入时间已确认 |
| `物流表相关爬取物流时间更新` | `柜号物流跟踪` | 按 `船东 + 提单号` 回填 `开船时间_爬取 / 预计到港日期_爬取 / 到港日期_爬取` | 上源当前键无重复；但下游视图 `物流明细表` 未选出这三个爬取字段 |

- 库存视图查询仍受 `utf8mb4_unicode_ci` / `utf8mb4_0900_ai_ci` 客户端排序规则差异影响；本轮用直接检查 `ship_out产品简码对应sku` 确认 30 个一对多映射仍在，不把本次客户端报错改写成新的停更根因。
- 本轮只读查看定义和聚合，没有调用任何过程。`normalize_simple_parts` 属于价格链；当前新增确认的 WFCG 函数属于利润发货费链，库存和物流链仍未引用函数。

## 2026-07-13 三条 ETL 边界继续确认

| 过程 / 视图 | 输入 | 输出 / 关键逻辑 | 结论 |
| --- | --- | --- | --- |
| `生成warehouse_stock_在售` | `warehouse_stock`、`公司sku上下架表` | 写入 2023-11-01 后库存并回补状态 / 分类，删除零件 / 分销 / 不统计 / 面料；停用 SKU 删除段已注释 | “在售”是品类过滤历史表，不是启用 SKU 严格子集；每日 04:05 后新增记录存在日内时差 |
| `link_sku_price_scj` | `平台产品详情表`、`osprice`、`wfprice`、`lowes_product` 等 | 每日 09:30 / 14:30 清空重建价格源；HD 当前分支写空价格，Lowes 取 `unit_prices` | HD 空价为过程设计结果；Lowes 空价来自源字段覆盖 |
| `上架sku利润表` | `link_sku_price_scj`、汇率、三国 DDP、发货费和 SKU 状态 | 12:42 生成；佣金 CASE 不含 WF-TD-UK，DDP 先精确 SKU 再去 `-E1` 回退 | 当日使用 09:30 价格批次；232 行缺 DDP 均未命中两级 DDP 键 |
| `入库单物流明细all` / `_2` | `物流表`、`仓库入库单`、`计划发货表` 等 | 开船 / 预计到港使用 `IFNULL(爬取, 人工)`，部分分支把爬取到港输出为实际到港 | 已把爬取日期传入部分库存、状态、DDP 和计划过程；与 `物流明细表` 日期逻辑不同 |

- 本轮仍未发现库存、利润、物流链调用真正 MySQL `FUNCTION`；唯一函数 `normalize_simple_parts` 继续只属于价格优势链。

## 2026-07-13 HD 价格回写与 UK 佣金规则差异

| 过程 / 视图 | 输入 | 输出 / 关键逻辑 | 结论 |
| --- | --- | --- | --- |
| `link_sku_price_scj` | `平台产品详情表`、OS / WF / Lowes 价格源 | 每日 09:30 / 14:30 重建；HD 当前写入 `原始价格=NULL, price=NULL` | HD 没有持续更新的上传价源；历史填报价分支已注释 |
| `ptcpxq_update_qtj_scj_price` | `link_sku_price_scj`、`link_sku_price_qtj_fabric` | 取每个产品最新非空上传价 / 本品前台价，分别回写 `平台产品详情表` | 只更新命中非空价格的产品，不会清除 HD 历史上传价；14:00 使用早于自身的 09:30 批次 |
| `link_sku_price_qtj_fabric` | 本品 / 竞品价格源，调用 `normalize_simple_parts` | 每日 09:30 / 14:25 重建前台价链 | 唯一 MySQL 函数仍只在此价格优势链；HD 本品前台价可刷新，不等于上传价 |
| `order_details_tb_profit` | `order_details_tb` | 视图对 `WF-TD-UK / WF-FOP-UK` 计算 `平台折后销售额=总销售额*0.94` | 订单层存在 6% 佣金候选规则 |
| `上架sku利润表` | `link_sku_price_scj`、汇率、DDP、发货费 | UK 佣金 CASE 未包含 UK；当前上游 `wfprice / link_sku_price_scj` UK 价格又全部为 0 | 与订单利润视图规则不一致；只补 CASE 不能形成有效当前佣金，正式价格与佣金选源待 FineBI / 业务确认 |

- `平台产品详情表`、`scj_price_adjustment_records`、`link_sku_price_scj` 的相关触发器只维护创建 / 更新时间，不承担价格或佣金公式。
- 本轮没有调用任何过程；以上关联来自只读过程、视图、事件定义和轻量聚合。

## 2026-07-13 Lowes 价格与订单佣金 ETL

| 过程 / 视图 | 输入 | 输出 / 关键逻辑 | 结论 |
| --- | --- | --- | --- |
| `all_latest_ptcp_update -> ptcpxq_product_stg` | `lowes_product` 最新 `create_time` 日期批次 | 更新 `平台产品详情表` 的 Lowes 产品、状态、链接 SKU / 上架 SKU | 每日 09:50 调用；商品映射过程已确认，源表外部同步待核实 |
| `link_sku_price_scj` | `平台产品详情表 INNER JOIN lowes_product ON 链接sku=sku` | 取 `unit_prices` 最后一个逗号分段，按源 `create_time` 日期写 Lowes 上传价历史 | 每日 09:30 / 14:30；最新日 2,404 行，其中 163 行价格空 |
| `link_sku_price_qtj_fabric` | `link_sku_table_2 LEFT JOIN lowes_product ON 产品id=LOWES_+store_unique_identifier` | 取同一价格字段写 Lowes 本品前台价，保留近 90 天源记录 | 每日 09:30 / 14:25；唯一函数调用位于同过程的 Overstock 竞品分支，不在 Lowes 分支 |
| `垂直后台订单` -> `垂直后台订单_temp` | `lowes_orders` 等垂直平台订单、`垂直上架sku映射` | 视图统一订单字段；每 2 小时落表并补采购 SKU / 数量，供 Shipout / CG 订单整合 | 运营订单支线已确认 |
| `zzdxq_details_stg_ddrq` | `总账单详情_details_stg -> 总账单详情_details` | 删除目标月自动抽取行后写 `order_details_tb` | 未直接引用 `lowes_orders`，也未发现库内事件或过程调用者；Lowes 进入 staging 的桥接待核实 |
| `order_details_tb_profit` | `order_details_tb` | Lowes 平台折后销售额为 `总销售额 * 0.85` | 视图公式与原始 15% 佣金一致；不是最终利润表 |

- `lowes_orders.order_number = order_details_tb.POstg` 当前命中 4,162 / 4,249 个订单，说明业务键高度一致，但不能替代物理装载证据。
- 该处 2026-07-13 的 87 单结论已被 2026-07-14 重算更新：当前为 92 单不守恒，16 单行数 / 数量放大、76 单形状相同但售价不同；不再将前一类解释为采购 SKU 一对多。
- `lowes_product`、`lowes_orders` 未见数据库业务触发器或直接事件。不要根据 Navicat 目录或表更新时间猜测同步程序。

## 记录标准

每确认一个过程，应补充：

1. 过程名称。
2. 输入表。
3. 输出表。
4. 运行方式和刷新时间。
5. 是否改变数据。
6. 失败时影响的报表或看板。
7. 相关脚本、SQL 或文档路径。
## 2026-07-13 SKU 身份与 WFCG 成本过程补充

| 过程 / 函数 | 主要输入 | 输出 / 应用 | 状态 |
| --- | --- | --- | --- |
| `采购sku_model映射表` | `公司sku上下架表`、`工厂订货表`、已有映射 | 每小时 :10 增量维护同名物理表；一采购 SKU 一行 | 已确认 |
| `link_sku_cr_2` | `平台产品详情表`、`采购sku_model映射表`、`os_manage_product`、`工厂订货表` | 每小时 :40 维护 `link_sku_table_2` 的本品链接、采购 SKU、Model 和产品编码 | 已确认 |
| `link_sku_table_2_删除重复的链接sku` | `link_sku_table_2` 本竞品关系 | 每小时 :26 删除竞品中的本品链接和重复竞品，并重写竞品产品 id | 已确认；并发与物理临时表风险待核实 |
| `all_latest_ptcp_update` | WF / OS / HD / Lowes 最新产品源、鲸汇销售产品 / WMS、公司 SKU 状态、`link_sku_table_2` | 每日 09:50 更新 `平台产品详情表` 的产品字段、采购 SKU、分类和 Model | 已确认 |
| `sku_model_cr` | 平台 KS / 订单、`link_sku_table_2` | 维护 `sku_model映射表` | 定义已确认；未发现当前事件 / 调用者 |
| `wfcg_classify` / `wfcg_conveyable` | 产品分类、长宽高、重量 | 为 `wfcg_calculate_all` 生成费率档位和可输送标识 | 已确认 |
| `wfcg_calculate_all` | Wayfair 上传尺寸 / 产品、平台采购 SKU、产品重量、`wfcg_rate_card` | 每日 10:20 全量重建 `wfcg_results` | 已确认 |
| `上架sku利润表` | 价格、汇率、DDP、产品 / 后台 / CG / WFCG 发货费 | 12:42 重建当日成本快照；HD / OS 取 WFCG Pick Only 候选，Lowes 取 Pick & Ship 候选 | 已确认数据库链；FineBI 最终选源待核实 |
| `wfcg_calc_ps_fee` / `wfcg_calculate_product` | 旧版价卡字段 | 当前无事件 / 调用者，定义与当前价卡列不兼容 | 不作为正式入口 |

更正：当前 schema 已有 4 个 FUNCTION。此前本文件中的“唯一函数为 `normalize_simple_parts`”只反映当时扫描结果；库存链仍未使用 FUNCTION，但利润发货费链已经使用 `wfcg_classify / wfcg_conveyable`。

## 2026-07-14 过程与应用补充

| 过程 / SQL 层 | 主要输入 | 输出 / 应用 | 边界 |
| --- | --- | --- | --- |
| FineBI `采购sku_model映射_2` SQL | `平台产品详情表`、预测事实、`公司sku上下架表`、`仓库每日销量断货情况_表` | FineBI 采购 SKU / Model 映射 | 不是数据库 PROCEDURE，也不是物理表；数据库侧复算已确认，FineBI 缓存待核实 |
| `客诉sku_model_cr` | 平台客诉、订单、`link_sku_table_2`、AMZ 产品管理 | 每日更新 `客诉sku_model映射表`，供 `all_ks_2` 使用 | 与 `sku_model_cr` 是两个独立过程；不能按名称包含关系误判调用 |
| `link_sku_price_scj` | `wfprice`、`osprice`、`lowes_product`、平台详情 | 重建上传价事实 | 目标 `日期` 直接取源日期，允许未来记录；新鲜度看 `CREATE_TIME` 和事件 |
| `上架sku利润表` 组合分支 | `link_sku_price_scj`、`鲸汇店铺销售产品最新`、三国 DDP、产品 / CG / WFCG 发货费 | 分组件聚合成上架 SKU 日成本 | 某组件 DDP 为空时，该组件发货费不进入 `成本`，但仍进入独立 `发货费` 字段；用 `无ddp采购sku` 识别 |

函数应用保持不变：库存和 SKU 身份 ETL 无 FUNCTION；`normalize_simple_parts` 属于价格优势链，`wfcg_classify / wfcg_conveyable` 属于 WFCG 发货费链，`wfcg_calc_ps_fee` 无正式调用者。

## 2026-07-14 价格利润与 Lowes 装载补充

| 过程 / 链路 | 输入 | 输出 / 关键逻辑 | 状态 |
| --- | --- | --- | --- |
| HD `link_sku_price_scj` 分支 | `平台产品详情表` HD 上架产品 | 为当日写入产品行，但 `原始价格 / price / 来源` 均为 NULL；历史 `scj_price_adjustment_records` 分支已注释 | 当前没有持续 HD 上传价；平台详情非空值是最晚 2024-05-30 的历史调整 |
| `ptcpxq_update_qtj_scj_price` | `link_sku_price_scj`、`link_sku_price_qtj_fabric` | 只取最新非空价格回写平台详情 | 14:00 仅消费 09:30 批次，不会清除 HD 历史价，也不会消费当天 14:25 / 14:30 批次 |
| `上架sku利润表` | 价格、汇率、DDP、发货费 | 佣金 CASE 未包含 `WF-TD-UK / WF-FOP-UK`；当前 UK 价格源为 0 | UK 佣金缺口同时受 CASE 和上游零价影响；未修改生产过程 |
| 库外按月批量装载器（待核实） | `lowes_orders` 或导出文件 | 按订单月份写 `总账单详情_details_stg` 并触发后续处理 | 批量形态已确认；当前库内过程、事件和项目脚本均未找到实现 / 调用者，是否人工未知 |
| `zzdxq_details_stg_ddrq` | `总账单详情_details_stg` | 按日期 / 月份替换详情和自动抽取订单，校验后写 `总账单详情_details -> order_details_tb`，最后清空 staging | 库内后半链已确认；不是 Lowes 源表装载器 |

- 2026-07-14 Lowes 对账为 92 单金额不守恒。16 单行数 / 数量放大，其中 9 单含完全重复目标行；76 单行数 / 数量相同但售价不同。84 单集中于 2026-07-08 装载批次。
- 2026-07-14 10:50 / 10:52 分别装载 2026-07 / 2026-05 月份，结合历史批次确认前置按月份批量送入；`总账单详情_details` 当前不再保留 2026-07-08 的 6 月批次，不能作为持久审计表。
- 当前 staging 为空是过程设计结果，不表示没有装载；没有当次 staging 文件 / 日志时，不能把 76 单明确归因于源修正或目标回刷。

## 2026-07-14 库存推送、看板与推仓过程补充

| 对象 | 类型 | 主要输入 | 写入 / 输出 | 调用与边界 |
| --- | --- | --- | --- | --- |
| `kczzkb_records_stg` | 过程 | 视图 `库存最终看板` | `库存最终看板记录留存` | 每日 23:55；按 `sku + date` 留存 |
| `库存推送推0店铺sku` | 过程 | `wayfair上架sku销售映射` 的 Not Live 和指定问题 SKU | 同名推 0 清单表 | 每日 07:15；当前展开 HD / OS / WF / Lowes 的 CG 推 0 行 |
| `活动提前调整虚拟库存_all_2` | 过程 | `warehouse_stock_最新`、`预计到仓表`、销售产品、订单、组合件、推 0 清单 | `库存推送sku虚拟库存设置`、`库存推送sku虚拟库存设置_历史记录` | 每小时 :35；组合件取最小可售组件数；历史只保留约 24 小时 |
| `销量预测填报表_总` | 过程 | 店铺、SKU 和月份范围 | `各店铺的sku发各仓库的占比` 骨架 | 每 2 小时；不写三个仓库占比字段，不能视为现行分仓比例计算器 |
| `鲸汇推仓仓库排序` | 总过程 | 库存、平台和仓库规则 | AMZ、Walmart、垂直、独立站仓库排序结果 | 每日 08:35；服务订单推仓，不是库存数量推送 |
| `鲸汇推仓特殊sku设置` | 总过程 | Lowes 尺寸 / 重量、库龄、单仓和人工规则 | `鲸汇推仓特殊sku设置` 及平台子规则 | 每日 08:50；人工原因不是自动规则时保留人工设置 |
| `库存借调分类` | 过程 | 同名视图、预计到仓拆分输入 | `库存借调分类_表`、`预计到仓库存拆分_表` | 两个启用事件重复调用；前者受 `库存拆分记录` 停更影响，后者仍可刷新 |
| `库存自动借调` / `kucuncf_jiediao*` | 过程组 | 借调分类、借调申请和库存拆分 | 借调申请 / 明细及执行结果 | 每日 06:00；与平台库存推送是独立链路 |
| `insert_kccf` | AFTER INSERT 触发器 | `执行借调申请表库存拆分` 新行 | 调用 `kucuncf_jiediao()` | 属于借调执行触发器，不是库存数量推送触发器 |

库外边界：当前数据库没有找到 `wf_submission库存变更记录`、`鲸汇库存推送执行结果`、`鲸汇推仓执行结果_python*` 的写入过程。数据库只能确认上游规则和结果持续存在，外部应用的读取优先级、API 调用及失败重试均 `待核实`。

## 2026-07-14 仓储费与利润过程补充

| 过程 / 触发器 / 事件 | 类型 | 输入 | 输出 / 影响 | 边界 |
| --- | --- | --- | --- | --- |
| `上架sku利润表` | 存储过程 | `wf仓储费用明细`、`cg_part_sku映射时间范围`、汇率、DDP 和其他发货费 | 生成每日 `上架sku利润表` | 只过滤 `charge_description='Fulfillment'` 形成 CG 后台发货费，不处理 Storage 仓储费 |
| `ccf_details_stg_2` | 存储过程 | `仓储费明细_old_2`、订单店铺映射 | 先清空再重建 `仓储费明细_stg_2` | 当前 old 表为空，目标费用和日期字段全空；禁止把它当现行费用装载链 |
| `ccf_details_stg_2` | AFTER INSERT 触发器 | `执行仓储费明细计算记录` 新行 | 调用同名过程 | 控制记录插入会触发清空 / 重建风险；不应手工执行 |
| `cg仓储费_temp` | 事件 | 定义调用 `cg仓储费_temp()` | 预期仓储费任务，实际结果对象不明 | 事件启用且每小时调度，但同名过程不存在；执行时间不能证明成功 |

未发现数据库定义直接写入 `ship_out_sku仓储费明细` 或 `mf_storage_fee_detail`，也未发现直接消费这两张表或视图 `cg仓每日仓储费` 的数据库对象；相应生产与应用血缘标注 `待核实`。

## 2026-07-14 广告采购 SKU 映射过程

| 过程 | 输入 | 中间对象 | 输出 | 关键规则与风险 |
| --- | --- | --- | --- | --- |
| `advertising_product_report_mapped` | `advertising_product_report_by_day`、`cg_part_sku映射时间范围`、三国 DDP | 临时表 `_tmp_ddp_adv`、`_tmp_ad_split`；持久表 `temp_advertising_product_report_by_day` | `advertising_product_report_mapped` | 拆最多 10 个 Part；映射必须命中店铺和生效期；DDP 按采购 SKU + 月份 + 国家左连；近 30 天 `DELETE + INSERT`。广告指标完整复制，结果不可直接汇总 |
| `advertising_product_report_by_day_stg` | `advertising_product_report_by_day_stg` | 无 | `advertising_product_report_by_day` | 以 `MD5(store+date+campaign+sku) + isB2b` 增量更新；staging 停在 2025-02，不是当前源表装载主链 |
| `temp_advertising_product_report_by_day` | `advertising_product_report_by_day` | 递归拆分 CTE | 同名持久中间表 | 旧独立拆分过程，无数据库事件调用；与新过程操作同名持久表，人工并发时存在竞态 |

## 2026-07-14 Amazon 销售与售后过程

| 过程 | 输入 | 输出 | 关键规则与风险 |
| --- | --- | --- | --- |
| `amz上架产品管理_2` | `领星erp亚马逊销售表` 最新日期批次 | `amz上架产品管理_2` | Upsert Listing 字段；不填采购 SKU / Model。事件刚启用但当日未执行 |
| `amz上架sku销售映射` | `_2`、`鲸汇店铺销售产品最新`、`amz上架sku销售映射_补全`、`产品最新ddp值` | `amz上架sku销售映射` | 无事务全量清空重建；未命中分支会写空采购 SKU / 数量到补全表；当前无事件 |
| `总账单详情_extract_amz` | 无 | 无 | 当前正文为空，不能作为 `amz_orders_allinfo -> order_details_tb` 已实现装载过程 |
| `amz_return_sal` | `领星erp亚马逊售后订单`、`order_details_tb`、鲸汇 / 旧 Amazon 商品映射 | `temp_amz_shouhou*`、`amz_return_sal` | 只处理退款；持久中间表；拆采购 SKU 后每行写完整 `售后数量 * 售价`；无事件 |
| `退货补件费用` AMZ 分支 | `order_details_tb`、`amz_return_sal`、`walmart_return_sal`、`store_bujian_sal` | `退货补件费用` | `平台=AMZ` 合并 Amazon + Walmart；销售国家 CASE 漏 AMZ 加拿大店铺 |
| `amz_ks_stg_2` | 退货分析、feedback、replacement、订单、采购 SKU / Model 映射 | `amz_ks_2` | 每日全量重建；评论不进入；店铺标准化漏 LJD-US 和加拿大 |
| `客诉sku_model_cr` AMZ 分支 | `amz_orders_allinfo`、`amz_ks_2`、`amz上架产品管理_new` | `客诉sku_model映射表` | 先于当日 AMZ 客诉重建运行；订单店铺标准化同样漏 LJD-US / CA |

业务触发器仅维护 `create_time / update_time`，不承载上述销售、退款或客诉公式。当前 Amazon 订单装载仍是库外边界。

## 2026-07-14 Overstock 库存与价格过程映射

| 过程 / 触发器 | 输入 | 输出 | 关键逻辑 / 边界 |
| --- | --- | --- | --- |
| `os_manage_cost_latest_stg` | `os_manage_product` 最新日期、历史 `os_manage_cost` | `os_manage_cost_latest` | Upsert 当前产品、状态、Supplier SKU 和基础成本；旧版直接读历史成本的代码已注释 |
| `all_latest_ptcp_update` | 多平台 current 产品过程 | 当前产品表，再调用 `ptcpxq_product_stg` | 每日 09:50 总控；OS current 主数据和平台详情同批更新 |
| `osprice_currdate_stg` | `os_partner_created_sales`、`datelist`、当日 `os_manage_cost` | `osprice` | 重建今天至 +10 天；活动最小有效价优先，基础成本只兜底当天 |
| `link_sku_price_scj` OS 分支 | `平台产品详情表`、`osprice` | `link_sku_price_scj` | 从 2025-07-18 起按产品 ID写 OS 上传价；保留最近三个月起的日期，包含未来计划价 |
| `delete_old_data` | `os库存变更记录` 等日志 | 删除设计保留期外日志 | 过程存在，但同名事件误调用不存在的 `delete_old__data()`；未执行修复或手工调用 |
| `osprice / os_manage_cost_latest` 时间触发器 | 新增或更新行 | 本表时间戳 | 只维护 `create_time / update_time`，不承载价格公式 |

未发现数据库过程、事件或业务触发器写入 `os库存变更记录`；该表生产端属于库外边界。

## 2026-07-14 库存库龄与店铺 FIFO 过程映射

| 过程 | 直接输入 / 子过程 | 输出 | 关键逻辑与边界 |
| --- | --- | --- | --- |
| `库存拆分总` | `订单出库数量`、`shipout每天库存`、`每天产品出库数量`、`每天剩余库存拆分新` | `每天剩余库存拆分`、`每天库存库龄拆分` 及 Shipout 中间结果 | 当前 CG 调用已从该总控注释，CG 由独立总控处理 |
| `库存拆分总_cg` | `cg仓库库存拆分`、`每天产品出库数量_cg`、`每天剩余库存拆分新_cg` | `每天剩余库存拆分_cg`、`每天库存库龄拆分_cg`、各仓拆分 | 先做剩余 / 库龄，再运行 `cg各仓库库存拆分` |
| `每天库存库龄拆分` / `_cg` | 对应剩余库存新表、上一批次库龄、手工调整和借调 | 对应库龄拆分表 | 连续非正库存可沿用上一批次库龄；正向手工调整写独立批次 |
| `sp_calc_all_stores` | Shipout 节点基线、店铺入库 / 出库、手工调整、借调 | `每天店铺库存数量_shipout` | 12 店铺逐个删除当日后重建；按入库时间 FIFO，库存不足时最新序号行可为负 |
| `sp_calc_all_stores_cg` | CG 节点基线、店铺入库 / 出库、手工调整、借调 | `每天店铺库存数量_cg` | 按 Part FIFO；同样保留欠货负数 |
| `入库单库龄明细表保存` | 视图 `入库单库龄明细_2` | `入库单库龄明细表`、`入库单库龄明细表_最新` | 删除当日快照后插入，再用最大快照时间重建 latest |
| `计划发货和在途拆分` | 同名视图 | `计划发货和在途拆分_t` | 每小时全量保存实时汇总；当前快照与实时视图一致 |
| `CG出库订单补充` | CG 库存变更销售负数、CG 订单和订单事实 | `各店铺cg仓出库订单` | 每日 21:27 补差；会创建 / 删除持久 `_tmp_cg_*` 表，失败时存在遗留风险，当前未见遗留表 |
| `cg_part_sku映射入库相关` | 生效期映射 | CG 店铺入库相关映射 | 每日 08:00 补新映射并延长当前映射截止日 |

本链没有调用 4 个真实 MySQL `FUNCTION`，目标结果表也没有业务触发器。`ship_out库龄` 只被 `入库单库龄明细_2` 消费，数据库内未发现写入过程、事件或触发器。

## 2026-07-14 销量预测填报与店铺分仓过程映射

| 过程 | 直接输入 | 输出 | 关键逻辑与风险 |
| --- | --- | --- | --- |
| `销量预测填报表_总` | 最新库存 SKU、`日均销量*`、产品分类 / 标签、各店铺填报 | 总量 / 垂直 / AMZ / 各店铺填报表、`销量预测填报范围`、仓库占比骨架 | 补月份骨架并回写分桶汇总；不填写各店铺值和三个仓库占比字段；分桶历史平台范围不一致 |
| `销量预测填报数量变动` | 三张月度填报表 | `销量预测填报数量变动` | `操作人 != 初始` 视为已填；统计预览差异和分桶为 0 异常，零分母差异率会为 NULL |
| `销量预估` | `销量预测填报范围`、三张月填报、订单历史、断货事实、当前库存 | `销量预估` | 每日删除重建诊断表；近 6 月序列无显式排序；不是普通断货预测直接输入 |
| `销量预估模块_amz` / `_垂直` | 对应每日销量、库存和在途 | 每日可用、历史断货和预测事实 | 分别有 08:40 / 08:50 活动事件 |
| `销量预估模块_tdcg` | TDCG 日销量 | `仓库每日销量断货情况_表_tdcg` | 未发现活动事件或调用者，按闲置 / 手工候选处理 |
| `各店铺未来三个月销量占比*` 三过程 | `销量预测填报表_各店铺`、近 90 天订单、产品分类 | 主表 / `_cg` / `_ca` 店铺销量与总销量，及分类销售额兜底表 | 每次删除当日后重建；CG / CA 分母含被注释的店铺输出，当前影响待核实 |
| `店铺库存拆分入库数量_shipout / _cg / _ca` | 对应三个月销量或分类销售额、入库数量 | 店铺入库拆分结果 | `CEIL` 初分后对超额行减 1；需先验证分子分母和唯一性 |

本链未调用 4 个真实 MySQL `FUNCTION`。相关填报表触发器仅见时间 / 操作字段维护线索，不作为预测或分仓公式来源。

## 2026-07-15 Wayfair 商品生命周期与 Part / SPO 过程映射

| 过程 / 触发器 | 直接输入 | 输出 | 关键逻辑与风险 |
| --- | --- | --- | --- |
| `all_latest_ptcp_update` | 多平台 current 源 | current 产品、`平台产品详情表` | Wayfair 依次调用 `wfproduct_management_latest_stg`、商品同步、采购 SKU、分类和 Model 更新；执行顺序已确认 |
| `wfproduct_management_latest_stg` | `wfproduct_management` 最新全局日期、`wf后台本品sku` | `wfproduct_management_latest` | 过滤空 Product Status 后按产品 id upsert；无序 `GROUP BY` 选择非聚合字段；CA / UK 未命中链接写 `无` |
| `ptcpxq_product_stg` Wayfair 分支 | `wfproduct_management_latest` | `平台产品详情表` | `Discontinued -> 停产下架`，其他非空状态 -> `上架`；下架日期回看最后非 Discontinued 历史日 |
| `ptcpxq_update_cgsku / category / model` | 鲸汇产品、`公司sku上下架表`、`link_sku_table_2` | 平台详情采购 SKU、分类、Model | 多采购 SKU 使用无序 `GROUP_CONCAT`，`采购sku_first` 不稳定；公司分类按每日批次回填 |
| `ptcpxq_abnormal_sku` | current 最近创建 / 更新时间 | 平台详情异常删除标记 | 与商品总控同在 09:50；当前完成后结果一致，但没有显式先后依赖 |
| `wf产品管理最新` | 中文 `wf产品管理`、平台详情、英文 Nonsellable Component、鲸汇产品 | `wf产品管理最新` | 清空重建；中文产品补采购 SKU / 链接 / Wayfair SKU，英文不可售组件单独补入 |
| `part状态最新_2` | 中文产品最新批次、鲸汇映射、英文 current、人工操作 | `part状态最新_2`、`需要创建spo的计划_2` | 逐店铺 upsert；当日人工操作跳过自动状态覆盖；只保留唯一采购映射；回写 Live Part 的 SPO 计划字段 |
| `gs_sku_sj_xj` | `公司sku上下架表` insert / update 触发器 | `sku对照表` | 即时传播采购 SKU 状态和分类；不直接更新平台详情 |
| `执行part状态最新` AFTER INSERT 触发器 | 人工插入执行记录 | 调用 `part状态最新_2()` | 人工触发入口；常规刷新仍由每小时事件执行 |

本链未调用任何真实 MySQL `FUNCTION`。`平台产品详情表`、两张 current 和 Part 表上的其他触发器主要维护创建 / 更新时间，不承载生命周期公式。

## 2026-07-15 Wayfair SPO 与刷柜过程映射

| 过程 / 视图 | 直接输入 | 输出 | 关键逻辑与风险 |
| --- | --- | --- | --- |
| `需要创建spo的计划_2` | `计划发货表`、物流、目标 / 备用仓、Part、交期、SPO 映射、创建 / 刷柜日志 | 计划主表及四店铺 shipment 头 / 明细 | 只选 CG、Z / P / X 类非散货计划；整批 Part 就绪才出 shipment；多处 Part 非空条件误用 OR |
| `目标仓库表` | 修改 Plan 日志、航行天数、产品 current | 目标仓配置增量、Part Wayfair SKU | 只发现新仓和更新延迟；调用 `备用仓库判定表()`，不自动设置目标 / 备选标记 |
| `备用仓库判定表` | 计划、预计到仓、断货预测、创建日志、交期 | 备用判断 current / 日留存 | 美西转美东 +32 天；整批通过才允许备用；留存先于当日更新 |
| `wfspo刷柜记录` | `修改plan日志`、重刷记录、SPO 映射、计划发货 | `wfspo刷柜记录` | 只整理外部已匹配结果并补运营保存状态，不执行平台刷仓 |
| `计划批次号刷柜次数` | 入库物流、计划主表、修改 Plan 日志 | 同名汇总表 | 统计各区域生成仓数量；结果表无唯一键且有历史双份行 |
| `重新刷柜记录` | 重刷申请、SPO 映射、计划 / 创建 / 刷柜日志 | 重刷申请状态、计划主表、刷柜记录 | 内容物改变会删除计划等待重建；过程没有数据库调度入口 |
| `wfspo创建计划明细` | 入库物流、计划主表、创建日志、刷柜记录 | 批次状态分类 | `cg仓入库单号` 优先判已创建 SPO；Plan 成功依赖旧日志关联键或刷柜记录 |
| `cg仓spo是否重新刷柜` | 当前计划、旧 SPO 内容、初始数量、实际仓库 | 重刷建议 | 新 Part 不同 Wayfair SKU或数量上涨至少 19%才触发；仓库不一致不进入布尔判断 |

本链未调用 4 个真实 MySQL `FUNCTION`。`创建plan日志` 只有时间戳触发器，数据库未发现写入创建 / 修改 Plan 日志、重刷申请或 SPO 映射的过程 / 事件。

## 2026-07-15 SPO 入库、标签与共享盘过程映射

| 过程 / 视图 / 触发器 | 直接输入 | 输出 | 关键逻辑与风险 |
| --- | --- | --- | --- |
| `warehousingentry_mf_shipout_stg` | `wayfaircg入库时间`、`wayfaircg入库`、Part 到采购 SKU 映射 | CG 入库临时、`仓库入库单` | 按运输状态和收货数量装载；两张 Wayfair 表是库外输入，不由本过程生成 |
| `下载spo标签填报表` | `下载spo标签文件日志` | 更新同名填报表 | 把日志 Part 数同时当总数和已下载数，不过滤 error / 空路径；无数据库调用者 |
| `共享盘cg_spo` | `仓库入库单` | 更新共享盘入库状态 | 按入库单号分组但状态未聚合，遇多状态可任取；无数据库调用者 |
| `更新物流表是否为第三方发货` | `共享盘cg_spo` | `物流表.第三方海运送CG仓` | 只传播第三方标记；源表停更且过程无数据库调用者 |
| `cg仓spo是否重新刷柜` | 当前计划、映射、`wayfaircg入库`、`ticket和spo创建` | 重刷建议视图 | ticket 计划数首段必须可转正数；`-` 会被过滤并失去初始量 |
| 入库时间触发器组 | `wayfaircg入库时间` insert / update | 同行日期字段 | 只把 `--` 转 NULL，不装载数据、不计算业务状态 |

本链没有 MySQL `FUNCTION`。数据库未发现写入 `wayfaircg入库*`、标签任务 / 日志、共享盘或 ticket 的过程 / 事件；只确认了消费和时间戳 / 清洗触发器。

## 2026-07-15 CG 入库到店铺库存过程映射

| 过程 / 触发器 | 直接输入 | 输出 | 关键逻辑与风险 |
| --- | --- | --- | --- |
| `warehousingentry_mf_shipout_stg` | `wayfaircg入库时间`、`wayfaircg入库`、`wf更改part_number`、`cg_part_sku映射时间范围`、CG 库存变更、计划 / 合并 SKU 辅助表 | `仓库入库单` | 状态和数量分支已确认；计划和映射在聚合前连接。使用普通全局中间表，存在并发覆盖风险 |
| `cg仓库库存拆分` | `_CG` 仓库入库、计划发货和 CG 计划映射、CG 库存变更 | `cg入库产品拆分_修改` | 首次写入后按差额追加；AWA Part 可直接归 AMZ，否则按计划比例拆；当前累计与仓库事实守恒 |
| `产品入库明细拆分` | 非 CG 仓库入库和计划发货 | `产品入库明细拆分` | 过程内 4 处排除 `_CG`；不得作为 CG 产品拆分生成者 |
| `店铺入库数量结果` | Shipout / SPO 计划、店铺销量与状态辅助对象 | `店铺入库数量结果` | floor + 余数分配并更新状态；每小时 :20 独立运行，晚于当前 CG 店铺总控开始 |
| `店铺库存拆分入库数量_cg` | CG 仓库 / 产品拆分、店铺入库结果、未来三月销量、分类销售额、第三方与手工入库 | 同名结果表 | 多分支 `INSERT IGNORE` 且无更新；存在性判断只看 `柜号 + Part`，上游后补量可留下旧值 |
| `cg仓店铺库存拆分总` | 上述店铺入库、手工调整和 CG 出库 | 调用店铺拆分、出库与 `sp_calc_all_stores_cg` | 调用顺序已确认；最后继续调用 CA 店铺拆分总控 |
| `sp_calc_all_stores_cg` | 节点基线、店铺入库、借调、CG 出库、手工调整 | `每天店铺库存数量_cg` | 循环 12 店铺做 FIFO；使用两张普通全局中间表，不是会话临时表 |
| `wayfaircg入库时间` 触发器组 | 源表 insert / update | 同行 7 个日期字段 | 7 个 INSERT + 7 个 UPDATE 触发器只把 `--` 转 NULL |
| `仓库入库单` 时间触发器 | 目标表 insert / update | `create_time / update_time` | 只维护时间戳，不计算状态、数量或映射 |

本链未引用 `normalize_simple_parts`、`wfcg_calc_ps_fee`、`wfcg_classify`、`wfcg_conveyable`；函数不参与 CG 入库入账。

### 2026-07-15 当前过程定义补充

| 过程 | 当前定义变化 / 已确认行为 | 剩余风险 |
| --- | --- | --- |
| `店铺库存拆分入库数量_cg` | 14:45 版本已在直接 SPO 分支的分配源和连接中加入 `柜号`；仍为多分支 `INSERT IGNORE`，无 UPDATE / DELETE | 仓库子查询仍未按柜号分组；所有 `NOT EXISTS` 仅看 `柜号 + Part`，已造成 14 个完整键、158 件跨入库单误拦截；目标主键也缺少入库单号 |
| `sp_calc_all_stores_cg` | 14:05 版本注释零库存过滤，12 店铺当日 FIFO 仍删除后重建；消费的是 `SUM(店铺入库数量)` | 56 个陈旧 `实际入库数量` 中 52 个不影响数据库 FIFO 总量，但影响审计 / 潜在库外读取；普通全局中间表并发风险仍在 |

过程修改与当天 5,256 行集中补写已观察到，但发布者、调用入口和补写方案无法由数据库定义确认，统一标注 `待核实`。

# FineBI 公共数据目录地图

最近追加日期：2026-06-25
接续学习路线复核：2026-06-25

## 范围与用法

- 范围：FineBI `公共数据` 下 `王威`、`tsq`、`章森淼` 三个负责人目录。
- 目的：先建立公共数据目录树事实地图，再与已学习文档做差集，后续只学习未覆盖的底层数据集、SQL、血缘和业务逻辑。
- 记录原则：本文件只记录目录结构、数据集名称、业务域判断和学习状态；指标口径、SQL 逻辑、血缘和坑点仍沉淀到对应业务文档。
- 状态说明：
  - `已学习`：已在广告、库存或业务文档中记录过 SQL / 字段 / 血缘 / 口径。
  - `部分学习`：只记录过目录或局部字段，仍需进入底层数据集补 SQL / 血缘 / 逻辑。
  - `待学习`：本轮新发现或尚未沉淀。
  - `待继续遍历`：目录树尚未完全展开到底层文件。

> 来源：2026-06-17 使用 Chrome 进入 FineBI `公共数据` 配置页，按目录树逐级展开读取。FineBI 目录树存在虚拟滚动，本文件会分批补全；未展开到底层的目录必须标注 `待继续遍历`。

## 遍历进度

| 负责人 | 当前进度 | 说明 |
| --- | --- | --- |
| 王威 | 进行中 | 已展开多批二级/三级目录，开始记录底层数据集；仍需补齐全树和差集状态。 |
| tsq | 进行中 | 已展开一级目录，并确认 `WF店铺曝光` 底层数据集；其余目录待展开。 |
| 章森淼 | 进行中 | 已展开一级目录；客诉、亚马逊评论、仓库、滞销等目录待继续展开到底层。 |

## 负责人一级目录地图

### 王威

业务域覆盖很广，已见一级目录如下：

- `WF产品`：已进入 `wf产品` 底层数据集补 SQL / 字段 / 更新 / 血缘；目录内是否还有其他底层文件待继续确认。
- `广告`：广告月度汇总、新广告等，广告专项已部分学习；本轮补充 `zzdnb_wf上架sku每日销售额`、`wf_sku不断货销量`。
- `wf_td近90天销售情况`：销售 / 近 90 天销量，已学习；底层文件为 `zzdnb_最近90天销量情况_td`，当前只见 1 个叶子数据集。
- `新广告`：WSD / WSS / WSV / Google Shopping 等新广告链路，广告专项已部分学习。
- `鲸汇关系换绑`：鲸汇店铺或 SKU 关系换绑，待学习。
- `废弃数据集`：含 `amz`、`垂直` 等子目录，存在售罄预估、订单、库龄、预测事实表等历史/废弃链路，待区分当前是否仍使用。
- `刷柜成功记录`：柜号 / 刷柜状态，待学习。
- `预估库存`：库存预测，待继续遍历。
- `wfspo创建计划`：Wayfair SPO / 计划，待学习。
- `仓库可发产品`：仓库可发，待学习。
- `计划发货表`：计划发货，待学习。
- `wf_组合产品`：组合产品，待学习。
- `店铺月利润`：销售 / 利润，待学习，可能关联广告利润缺口。
- `退货`：退货 / 客诉相关，待学习。
- `上架sku利润`：上架 SKU 利润，待学习，可能关联广告利润缺口。
- `总账单不对数据`：账单差异 / 财务异常，待学习。
- `ddp利润表`：DDP / 利润，待学习，可能关联通用利润口径。
- `采购sku_model`：SKU / Model 映射，库存专项已部分学习。
- `物流表`：物流 / DDP / 发货费，待学习。
- `库存调整`：库存调整，库存专项已部分学习。
- `公司内部排位`：经营排位，待学习。
- `wayfair产品前台价格变动`：价格变动，待学习。
- `本品竞品`：本品竞品价格，广告专项已部分学习。
- `价格调整`：价格调整，待学习。
- `库龄拆分`：库龄，库存专项已部分学习。
- `预计到仓表`：预计到仓，库存专项已部分学习。
- `库存拆分相关`：库存拆分，库存专项已部分学习。
- `物料管理`：物料，待学习。
- `预估月销售罄表`：售罄预估，库存专项已部分学习。
- `仓储费`：仓储费 / shipout，每日库存，库存专项已部分学习。
- `自动借调`：库存借调，库存专项已部分学习。
- `增量更新`：广告 / OS / WF 等增量 SQL，广告专项已部分学习。
- `评论观点`：评论 / 观点 / 商品反馈，待学习。
- `相似品`：相似品，待学习。
- `断货天数`：断货天数，库存专项已部分学习。
- `销售详情`：销售明细，待学习。
- `客户`：客户 / 客诉候选，待学习。
- `测试`：测试目录，低优先级。
- `店铺绩效`：WF / OS / HD 店铺绩效，广告利润上游待重点学习。
- `库龄大于10天且当前库存大于最近三个月最大销量`：库龄 / 滞销候选，库存专项已部分学习。
- `库存和预计到仓情况`：实时库存与预计到仓，库存专项已学习。
- `库龄超80天数据`：库龄，待学习。
- `市场大盘趋势`：市场 / 评论 / 产品趋势，待学习。
- `上下架提醒`：上下架提醒，待学习。

### tsq

已见一级目录如下：

- `送测情况`：送测，待继续遍历。
- `曝光情况`：曝光，待继续遍历。
- `流量情况`：流量，待继续遍历。
- `发货销售情况`：发货 / 销售，待继续遍历。
- `市场`：市场，待继续遍历。
- `WF店铺曝光`：广告曝光 / BID，已部分学习。
- `ddp利润表`：DDP / 利润，待学习。
- `ddp明细表`：DDP 明细，待学习。
- `本品竞品`：本品竞品，待学习。
- `CA_stock`：加拿大库存，待学习。
- `CA订单明细`：加拿大订单明细，待学习。

### 章森淼

已见一级目录如下：

- `wf产品属性检查`：WF 产品属性，待继续遍历。
- `WF后台PENDING名单相关`：WF 后台 pending，待学习。
- `客诉相关_2`：客诉，待重点学习。
- `亚马逊评论`：AMZ 评论，待重点学习。
- `订单物流状态跟踪相关`：订单 / 物流状态，待学习。
- `仓库销量可用预估2`：仓库销量与可用预估，库存专项已部分学习。
- `滞销sku列表`：滞销 / 清仓候选，广告专项已部分学习。
- `仓库库存可用预估`：仓库库存可用和断货，库存专项已学习。
- `仓库存相关`：仓库存，库存专项已部分学习。
- `客诉相关表`：客诉，待重点学习。
- `公司`：公司基础数据，待学习。
- `垂直平台`：垂直平台，待学习。

## 已展开底层文件节选

### 王威 / WF产品

- `wf产品`：部分学习，已确认 FineBI SQL、预览字段、更新记录和一层血缘；下游进入 `zzdnb_detailed_listing_health`、`zzdnb_wf_supplier_parts_performance`、`wf产品详情`，更深节点待继续展开。

### 王威 / 广告

- `WF广告汇总`：已学习，广告月度汇总 SQL 已记录到广告专项；2026-06-18 复核确认下游血缘数字 `10`。
- `总广告数据`：已学习，`store + 年月` 粒度；血缘进入整体广告组件和广告组主题链路，SQL 未直接展示；2026-06-18 复核确认下游数字 `总广告数据_广告组数据=109`、`总广告数据=14`。
- `分类目广告数据`：已学习，`store + 年月 + 分类` 粒度；血缘进入分类广告组件和广告组标签拆分链路，SQL 未直接展示；2026-06-18 复核确认下游数字 `分类目广告数据_广告组数据=109`、`分类目广告数据=20`。
- `zzdnb_wf上架sku每日销售额`：部分学习，已确认预览字段、更新记录和血缘进入 `广告` 自助表；FineBI 未展示可复制 SQL。
- `wf_sku不断货销量`：部分学习，已确认 SQL 为 `select * from wf_sku不断货销量`，血缘进入 `广告` 自助表，并继续到 `总广告数据`、`分类目广告数据`、`WSP广告组每月汇总` 和多组广告组件。

### 王威 / wf_td近90天销售情况

- `zzdnb_最近90天销量情况_td`：已学习。FineBI 编辑页显示为表型数据集，表名同数据集名；数据预览为 SKU 粒度近 90 天销量和 30 天分段均价，2026-06-17 07:16 更新后 `1,294` 行；血缘为 `zzdnb_最近90天销量情况_td -> zzdnb_最近90天销量情况_td1 -> 组件 -> 仪表板`，具体组件 / 仪表板名称待核实。

### 王威 / 新广告

- `zzdnb_sponsored_shops_sku_level_attribution`：部分学习。Sponsored Shops SKU attribution 表型数据集；字段、更新任务和一层血缘已确认，底层 SQL / 具体组件仍待核实。
- `zzdnb_sponsored_shops_shop_performance`：部分学习。Sponsored Shops shop performance 表型数据集；字段、更新任务和一层血缘已确认，是 `WF广告汇总` 中 `WSS` 来源候选，底层 SQL / 汇总关系待核实。
- `zzdnb_google_shopping_sku_level_attribution`：部分学习。Google Shopping SKU attribution 表型数据集；字段、更新任务和一层血缘已确认，`Retail Revenue` 与 `Wholesale Revenue` 需区分，底层 SQL / 具体组件仍待核实。
- `zzdnb_google_shopping_enhanced_attribution`：部分学习。Google Shopping Enhanced Attribution 表型数据集；字段、更新任务和一层血缘已确认，Direct / Assisted attribution 字段已见，底层 SQL / 具体组件仍待核实。
- `zzdnb_google_shopping_attribution_new_to_wayfair`：部分学习。Google Shopping New to Wayfair attribution 表型数据集；字段、更新任务和一层血缘已确认，`NTW Orders` 字段口径和下游组件仍待核实。
- `zzdnb_google_shopping`：待学习。
- `zzdnb_sponsored_video_sku_level_attribution`：待学习。
- `zzdnb_sponsored_video_class_performance`：待学习。
- `zzdnb_sponsored_display_sku_level_attribution`：待学习。
- `zzdnb_sponsored_display_new_sku_level_attribution`：待学习。
- `zzdnb_sponsored_display_new_sku_class_performance`：待学习。
- `zzdnb_sponsored_display_class_performance`：待学习。

### 王威 / 店铺绩效 / WF

- `zzdnb_advertising_product_report_by_day`：已学习，WF 广告日报抽取入口，2026-06-17 复核行数 / 更新记录 / 部分血缘。
- `zzdnb_detailed_listing_health`：部分学习，Listing health 月度数据，广告主题用于补 `评论数` / `评分`；源字段为英文 review/rating 字段。
- `zzdnb_广告组合单买详情`：部分学习，组合链接 / 单卖辅助映射，需继续确认是否直接进入广告组分析。
- 其他本轮在目录中看到但未展开：`zzdnb_wf_class中文分类`、`zzdnb_鲸汇店铺销售产品`、`option_drill_down`、`zzdnb_option_drill_down`、`account_overview_conversion`，待学习。

### tsq / WF店铺曝光

- `model`：待学习。
- `zzdnb_wf广告出价表`：已学习，广告 BID 快照，已记录到广告专项和看板映射。
- `wf店铺自然流数据`：待学习，可能关联自然曝光 / 自然访问。

### tsq / 发货销售情况

- `新鲸汇发货仓库`：待学习。
- `发货仓库`：待学习。

### tsq / 市场

- `product_fabric_review`：待学习，评论 / 市场数据候选。
- `英国评论`：待学习。
- `reviewerlocation判断-英国`：待学习。
- `crateandbarrel_product`：待学习，竞品商品数据候选。
- `crateandbarrel_review`：待学习，竞品评论数据候选。
- `yardbird_product`：待学习，竞品商品数据候选。
- `yardbird_review`：待学习，竞品评论数据候选。

### tsq / ddp利润表

- `美西非CG和美西总ddp均值`：待学习，DDP 均值口径候选。
- `CG备用发货费`：待学习。
- `CG发货费`：待学习。
- `90670发货费`：待学习。
- `ddp汇总`：部分学习，SQL 为美国/加拿大/英国 `ddp汇总表_*` union，已确认进入广告利润相关血缘，仍需继续展开完整下游。

### tsq / ddp明细表

- `实际ddp`：待学习。
- `ddp明细`：待学习。

### tsq / 本品竞品

- `前台价-最新`：已学习。SQL 为从 `link_sku_price_qtj_fabric` 按 `产品id` 取最新日期行；血缘进入 `前台价-最新天`、`本竞品价格对比`、`价格明细`。
- `本品链接`：已学习。SQL 为 `link_sku_table_2` 中 `产品类型="本品"` 的 distinct `link_sku`。
- `model_num`：待学习。
- `垂直平台各链接sku销量`：待学习，销售候选。
- `竞品最小价`：已学习。SQL 取 `link_sku_price_qtj_fabric` 中 `产品类型="竞品"` 的最新日期行，并左连 `link_sku_table_2` 补 `竞品备注`；该数据集名称含“最小价”，但 SQL 层未直接 `MIN(price)`，最低价需在后续聚合/组件或物理过程确认。
- `上架日期半年内`：待学习。
- `排除链接sku`：待学习。
- `不同日期前台价变化`：待学习。
- `link_sku_price_scj`：待学习。
- `link_sku_price_qtj_fabric`：部分学习。FineBI 预览为 SQL 数据集 / 价格底表镜像，字段含 `产品id`、`采购sku`、`上架sku`、`link_sku`、`platform`、`store`、`price`、`日期`、`model`、`产品类型` 等；数据库物理表是 `产品近两天的最小前台价` 的核心上游。
- `review`：待学习，评论候选。
- `价格明细`：部分学习。已见其左连 `平台产品详情表` 补 `链接sku/model/平台sku/上架日期/上下架状态`，并左连 `product_fabric_review` 取首次上架日期；过滤近 3 天更新数据，OS/LOWES 本品需 `上下架状态="上架"`。

### tsq / 根目录直接数据集

- `CA_stock`：待学习，加拿大库存候选。
- `CA订单明细`：待学习，加拿大订单 / 销售候选。

### 王威 / 废弃数据集 / amz

- `每月销量售罄预估表_amz`：待学习，需确认是否仍使用。
- `zzdnb_tb_kyxl_forecast_and_reality_amz`：待学习，需确认是否仍使用。

### 王威 / 废弃数据集 / 垂直

- `计划发货和在途`：待学习。
- `每月销量售罄预估表_垂直`：待学习，需确认是否仍使用。
- `后台订单详情`：待学习。
- `wfspo创建计划`：待学习。
- `cg后台仓库订单`：待学习。
- `shipout后台仓库订单`：待学习。
- `zzdnb_总出库订单`：待学习。
- `zzdnb_tb_kyxl_forecast_and_reality_垂直`：待学习，需确认是否仍使用。
- `库龄明细`：待学习。
- `库存超45天每日明细`：待学习。
- `zzdnb_库龄超45天明细`：待学习。

### 王威 / 刷柜成功记录

- `刷柜成功记录`：待学习。
- `刷柜没有成功的批次`：待学习。
- `刷柜成功记录—直连`：待学习。

### 王威 / 市场大盘趋势

- `wayfair` 子目录：待继续学习。
  - `WF平台斜率`
  - `zzddata_wayfair_product`
  - `zzddata_wayfair_meiyue_pinglunshu`
- `noon` 子目录：待继续学习。
  - `noon备注斜率`
  - `zzddata_noon_product`
  - `zzddata_noon_meiyue_pinglunshu`
- `wf子平台市场调研` 子目录：待继续学习。
  - `备注斜率`
  - `zzddata_市场调研表`
  - `zzddata_wayfair_sub_platform_product`
  - `zzddata_wayfair_sub_platform_pinglunshu`
- 其他已见底层：
  - `HD近三个月有评论的商品`
  - `HD平台斜率`
  - `zzddata_homedepot_meiyue_pinglunshu`
  - `zzddata_homedepot_product`
  - `zzdnb_product_main_image`
  - `zzdnb_link_sku_table_2`
  - `OS最近三个月有评论数量的商品`
  - `OS平台斜率`
  - `zzddata_overstock_meiyue_pinglunshu`
  - `zzddata_overstock_product`

### 王威 / 上下架提醒

- `上下架提醒_独立站`：待学习。
- `上下架提醒`：待学习。
- `zzdnb_公司sku上下架表`：库存专项已部分学习。
- `zzdnb_上下架提醒sku排除表`：待学习。
- `美工确认表`：待学习。

### 章森淼 / wf产品属性检查

- `带星号的无回答属性的相关统计`：待学习。
- `wf属性检查筛选`：待学习。
- `警告属性统计`：待学习。
- `wf产品属性检查`：待学习。

### 章森淼 / WF后台PENDING名单相关

- `wf后台pending名单`：待学习。

### 章森淼 / 客诉相关_2

- `差评底表_2去重`：待学习。
- `all_ks_2`：待学习，客诉合并主表候选。
- `亚马逊replacement_tracker底表_2`：2026-07-14 数据库侧已学习。当前 380 行，与 AMZ replacement 源按 `po_number + 采购sku` 一对一；FineBI 是否额外过滤待核实。
- `领星erp亚马逊退货分析表底表_2`：数据库侧为源表直接标准化视图，当前 12,739 行 / 12,739 个 `extend`，一对一。
- `领星erp亚马逊反馈表底表_2`：数据库侧已学习。商品 JSON 展开后按订单号连接订单行，当前 7 个业务键重复、9 条额外行；FineBI 使用前需确认去重。
- `客诉底表筛选_amz`：数据库侧可执行，合并退货、feedback、replacement 三类键，当前 6,997 行；实际组件过滤待核实。
- `店铺每月差评率_amz`：定义已确认，但当前执行报排序规则冲突；不可作为现行直接取数入口。
- `差评底表_2_amz`：数据库侧筛选 1-3 星并拆 MSKU；当前 512 行中有 23 条 ASIN 关联放大行，需去重 / 修正关联键。
- `客诉底表筛选`：待学习。
- `差评底表_2`：待学习。
- `wayfair订单补货记录底表_2`：2026-07-13 数据库侧已学习。与无后缀视图定义完全一致；从 `wf_ks_2` 的 Wayfair 补货客诉出发，按 `store + PO` 连接后台 replacement order，并补 Model / 采购 SKU。一个 PO 可对应多个 replacement order，当前存在候选键重复，FineBI 是否额外去重及实际选用哪个同义视图待核实。
- `wayfair后台客诉底表_2`：待学习。
- `my_tickets底表_2`：待学习。
- `客诉表填报表底表_2`：待学习。
- `overstock_return底表_2`：待学习。
- `hd_special_orders底表_2`：待学习。
- `overstock_performance底表_2`：待学习。
- `店铺每月差评率`：待学习。
- `店铺每月客诉率`：待学习。

### 章森淼 / 亚马逊评论

- `领星erp亚马逊退货分析表`：数据库源字段、粒度和退货底表视图已确认；FineBI 当前过滤与展示字段待核实。
- `数据连接_amz上架产品管理`：旧商品管理链每日 08:30 / 08:45 刷新并供客诉 / 评论映射；FineBI 是读旧表、`_new` 还是新 `_2` 待核实。
- `领星erp亚马逊评论相关`：数据库候选为 `领星erp亚马逊评论表 -> 差评底表_2_amz`；当前差评阈值为 1-3 星，存在 ASIN-only join 放大。
- `领星erp亚马逊反馈相关`：数据库候选为 `领星erp亚马逊反馈表 -> 领星erp亚马逊反馈表底表_2`；订单行连接会产生少量重复，FineBI 去重待核实。

### 章森淼 / 订单物流状态跟踪相关

- `物流明细`：2026-07-13 数据库侧部分学习。物理候选为视图 `物流明细表`，由 `物流表 + 发货明细表` 按柜号展开，并合并指定 `other_warehousingentry_stg` 来源；候选粒度为 `来源 + 计划批次号 + 柜号 + sku`。FineBI 是否直接使用该视图、是否增加日期 / 状态过滤仍待核实。
- `订单物流状态跟踪表`：2026-07-13 数据库字段、主键、来源窗口、同名过程和两小时事件已确认。物理粒度为 `订单号 + 物流单号`；当前状态不能只看插入时的 `物流状态`，自动轨迹候选为 `物流详细信息状态`，手工 `物流状态填报` 的优先级待 FineBI 组件确认。

### 章森淼 / 仓库销量可用预估2

- `zzdnb_tb_kyxl_forecast_and_reality_合并sku`：已学习，库存专项已记录。
- `各sku每月断货天数`：已学习，库存专项已记录。
- `zzdnb_断货sku数量_合并`：已学习，库存专项已记录。

### 章森淼 / 滞销sku列表

- `滞销品列表`：部分学习，广告专项已记录 SQL 片段和滞销判断逻辑；仍需确认具体广告看板引用关系。

### 章森淼 / 仓库库存可用预估

- `zzdnb_断货sku数量`：已学习，库存专项已记录。
- `各sku每月断货天数`：已学习，库存专项已记录。
- `采购sku_model映射_2`：已学习，库存专项已记录。
- `预计到仓表`：已学习，库存专项已部分记录。
- `数据连接_tb_kyxl_forecast_and_reality`：已学习，库存专项已记录。

### 章森淼 / 仓库存相关

- `cg仓库存`：已学习，库存专项已记录。

### 章森淼 / 客诉相关表

- 子目录 `客诉-王威移动过来`：待学习。
  - `zzdnb_柏礼sku`
  - `筛选`
- `各采购sku整体客诉率`：2026-07-10 数据库定义已复核；旧垂直平台口径，当前因排序规则冲突不可执行，FineBI 当前是否仍缓存 / 使用待核实。
- `采购sku按店铺来源细分客诉率`：2026-07-10 数据库定义已复核；当前因排序规则冲突不可执行，且只输出有客诉的店铺 / SKU / 月 / 来源组合。
- `all_ks`：旧物理合并表，业务日期仅到 `2026-04-17`；当前跨平台事实优先候选为物理视图 `all_ks_2`，FineBI 数据集对应关系待核实。
- `my_tickets底表`：待学习。
- `wayfair后台客诉底表`：待学习。
- `wayfair订单补货记录底表`：2026-07-13 数据库侧已学习。当前与 `_2` 视图定义完全一致；可用于补件原因、状态、部件和客诉日期明细，但按 `store + PO` 连接存在一对多放大风险，未经 FineBI 去重规则确认不能直接汇总 `qty`。
- `overstock_return底表`：待学习。
- `overstock_performance底表`：待学习。
- `客诉表填报底表`：待学习。
- `hd_special_orders底表`：待学习。
- `合并所有来源的底表2.0`：跨平台客诉合并候选；数据库 `all_ks_2` 已确认汇总 WF / HD / OS / AMZ，FineBI 是否直接对应本数据集待核实。
- `差评底表`：数据库当前可用候选为 `差评底表_2去重`；按 `website + reviewId` 去重且标准化评分 `<5`，正式差评阈值待核实。
- `各model整体差评率`、`按店铺来源细分差评率`：FineBI 数据集存在；当前物理同名对象未在本轮确认，继续待核实。
- `按店铺来源细分客诉率`：FineBI 数据集存在；物理候选 `采购sku按店铺来源细分客诉率` 当前不可执行，精确对应关系待核实。
- `各model整体客诉率`：数据库视图定义已复核；旧垂直平台口径且当前因排序规则冲突不可执行。

2026-07-10 第一轮数据库结论：当前客诉事实入口、差评去重入口、两类客诉率月份口径和每日事件 / 过程已确认；FineBI 组件实际使用哪个数据集、是否保留迁移前缓存结果仍待进入 FineBI 页面核实。

### 章森淼 / 公司 / 检查日常总账单填报和总账单详情

- 子目录 `产品销售报表相关数据集`：待学习。
  - `zzdnb_海外仓发货运费`
  - `采购sku_model映射`
  - `zzdnb_平台产品详情表`
  - `发货费`
  - `修改postg后的全部订单`
  - `order_details_tb_tj`
  - `zzdnb_order_details_tb`
  - `zzdnb_sku对照表`
  - `zzdnb_ptfl_stg`
- 子目录 `订单日期维度_2 / 文件夹`：待学习。
  - `填报表和总账单明细差别(订单日期)_details`
  - `总账单和填报明细差额(按天订单日期)_details`
  - `总账单和填报明细差额(按店铺按天订单日期)_details`
  - `总账单详情日期store维度`

### 章森淼 / 垂直平台

- `各店铺自行送测记录`：待学习。
- `order_details_tb_profit`：部分学习，订单利润明细候选；已见字段、行数、DDP 拆分血缘和 `ddp_利润查询` 关联视图。
- `wayfair_orders_allinfo`：待学习，WF 订单信息候选。
- `zzdnb_最新价格表`：待学习。

## 差集学习优先级

1. 利润相关：`王威/店铺月利润`、`王威/上架sku利润`、`王威/ddp利润表`、`tsq/ddp利润表`、`tsq/ddp明细表`、`王威/店铺绩效/WF`。
2. 客诉/评论相关：`章森淼/客诉相关_2`、`章森淼/客诉相关表`、`章森淼/亚马逊评论`、`王威/退货`、`王威/评论观点`、`王威/客户`、`王威/市场大盘趋势`。
3. 销售相关：`王威/销售详情`、`王威/wf_td近90天销售情况`、`tsq/发货销售情况`、`tsq/CA订单明细`。
4. 库存补充：`tsq/CA_stock`、`王威/预估库存`、`王威/自动借调`、`章森淼/仓库存相关`。

## 2026-06-25 接续学习说明

- 本次复核只确认学习路线和已沉淀入口，不表示上述 `待学习` 数据集已经完成 SQL、字段、公式或血缘确认。
- 后续进入 FineBI 前，先读 `docs/playbooks/FineBI业务学习与血缘字段复核流程.md`，再按本地图选择一个数据层入口。
- 优先学习“能解释多个报表或已知缺口”的底层数据集：利润 / DDP / 发货费优先级最高，客诉 / 评论次之，销售和库存补充按具体需求推进。
- 每进入一个数据集，都要补齐：FineBI 路径、SQL 或无 SQL 状态、字段清单、更新时间、右侧流程、公式列、添加列匹配键、上游 / 下游血缘、样本复核和待核实项。
- 仪表盘、Tab、组件画布只记录为入口或下游终点；除非它能反向定位到未确认字段来源，否则不作为学习主线。
